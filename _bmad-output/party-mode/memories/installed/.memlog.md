---
updated: 2026-08-06T17:14
---

- (event) Party review (Samus game-design + Sally UX) on the hotel-rating cutoff change (v1.17.1): hotel rooms & suites now count toward the star-rating population up to 4★ (`ratingPopulation()`/`hotelsCountTowardRating()` flipped `star < 3` -> `star < 4`), so they help reach 4★ but are excluded for 5★/TOWER. STAR_THRESHOLDS and TOWER_POPULATION untouched; only the 4★ bar is softened.
- (outcome) Samus 🟢 SHIP: implementation faithful to the approved plan, PARITY.md must-fix landed (census line now "up through 4★"), and NO sibling star gate got swept to 4 (verified untouched: Santa `star<3`, recycling `star>=3`, office/suite parking, VIP `star<3`). Balance intent intact ("a real city needs real residents, not a motel", pushed one star later). One DEFERRED design corner: `ratingPopulation()` keys off the CURRENT star, so a 3★ tower clearing both the 4★ and 5★ pop bars in one `evaluateStar` tick counts hotels for that whole 3★->5★ leap; narrow (5★ metro + amenity gates), documented in-code as an accepted corner mirroring the old 3★ seam, backlog-logged for possible per-star recomputation.
- (outcome) Sally UX TWEAK -> patched: the stats-modal footnote said "up through 4★" while the inspector said "stop counting at 4★" -- a digit-level contradiction that renders exactly when hotels have already dropped out, the reading that makes players think it's a glitch. Rewrote the footnote to "count toward your star rating up to 4★; from 5★ on, only permanent residents count. Your rating won't drop, and hotels still earn income." -- adds the no-demotion + still-earns-income reassurance so nobody panic-bulldozes hotels at the 4★ cliff. Inspector line ("stop counting at 4★ (they still earn income)") shipped as-is. Deferred: the "next star"/"stars" label drift across the three surfaces (standardize on "Counts toward stars" if ever touched).

- (outcome, MARATHON) TDT EXPORT now LOADS AND PLAYS in the real 1994 game (v1.14.0), closing the export half of `tdt-trailing-structure-layout`. A full 2★ my_tower (offices/hotels/condos/fast food, 3 shafts, 6 stairs) renders and runs in the Wine harness with no 0x0799 crash. Cracked by ISOLATION: blanking each trailing structure in the game's OWN save to see which the game actually needs on load. Four root causes, in order: (1) FILE TOO SHORT -- the game reads a fixed trailing routing region after the stairs table and overran our short file; emit it 0xFF-filled to TDT_ROUTING_TAIL_SIZE (0xFF = the empty-slot sentinel; ZERO-fill instead is read as live data and invents a phantom Pop 1280 + bogus star -- proven with EXPFF vs EXPPAD). (2) PEOPLE COUNT 0 faults a POPULATED tower (empty tower is fine at 0) -- forcing the game's own save to people=0 crashed it; keeping the count and ZEROING every record loaded fine, so write the resident/worker census (residentCount) with that many zeroed records. (3) ZERO ELEVATOR SCHEDULE BLOCK loads shafts with NO CARS (people trapped) -- the 56-byte block @4 of each header; the game's shafts all carry the default 01x14 05x14 00x28, emit that (TDT_ELEVATOR_SCHEDULE_DEFAULT). (4) VIEW opened on the sky -- write the New Tower view-scroll default at header 0x26/0x28 (1105,3491) so it opens on the ground lobby. RULED OUT as NOT required: the per-floor 94-entry index maps (zeroing them in the game's save still loads) and the post-stairs §11 content (0xFF works). Elevator payload formula (3140+324*sf+348*cars) independently CONFIRMED byte-exact against the game's my_tower shafts. Owner drove the last two finds live ("NO ELEVATORS HAVE CARS! people trapped"; "camera should load to the proper place"). Guarded by 6 new tests in tdtExport.test.ts. Files: tdtExport.ts (people census + 0xFF tail + schedule + camera), tdtFormat.ts (TDT_ROUTING_TAIL_SIZE / TDT_MAX_CENSUS / TDT_ELEVATOR_SCHEDULE_DEFAULT / TDT_DEFAULT_VIEW_X/Y), canon doc §1/§6/§8/§11. Remaining (backlog `tdt-export-routing-tail`): the fixed tail size is 2★-validated only; 3★+ needs real saves to pin the size law.
- (outcome) Round-trip tooling (vctower-to-tdt / tdt-to-vctower) switched from flaky Vite SSR (was timing out on the 60s module-runner transport this whole session) to direct engine imports under `tsx` (.mjs -> .ts). Fixes the tooling AND justifies the `tsx` devDependency Copilot flagged as unused.

- (outcome) Debated adopting Excalibur's ex.Serializer for save/load vs keeping headless Simulation.serialize(). Room converged: keep Simulation as source of truth; ex.Serializer targets the render/ECS layer which here is derived/regenerated cache state. Decisive point (Boundary) = current deserialize() hardens against hostile/corrupt saves (NaN clamp, unknown-kind drop, zero-height shaft guard) which the generic serializer can't.
- (dynamic) Grumbal opened hostile ('NIH, you reinvented the engine serializer') and only conceded to Boundary's corruption-hardening argument, not Winston's 'shadow on the wall' poetry, told Winston to lead with substance. Dana kept forcing the user-impact question. Boundary is the room's quiet closer.
- (outcome) Follow-up decision after PR #38 merged: build (1) round-trip stability test serialize->deserialize->serialize with non-empty transport car arrays + unknown-version-doesn't-throw, and (2) an HONEST one-line version seam in deserialize (read data.version, call migrate() = identity for v1 with anchor). Explicitly NOT a migration framework.
- (dynamic) John (PM) walked in and killed the gold-plating: 'why build a migration framework for a v2 that may never come?' Grumbal sided with him ('museum exhibit that rots'). Boundary saved the idea by redefining it as a one-line SEAM not a framework, 'version:1 is a lie nobody reads.' Winston conceded Boundary made his argument better. John+Grumbal+Boundary alliance against premature machinery.
- (outcome) Implemented & shipped the seam+test decision: SAVE_VERSION const, migrateSave() identity-for-v1 seam in deserialize(), round-trip + unknown-version tests. All 99 tests green, typecheck+lint clean.
- (outcome) Design panel (Sally UX, Samus game-design, Cloud architect) redid the Tower Statistics modal: killed the sunken white sheet (.stats-grid was lumped into the textarea white-well rule), sections became navy beveled mini title bars, milestones got an evalbar progress gauge, funds/stars echo HUD colors, and every dialog title bar + the mobile inspector card gained a Win-style ✕.
- (dynamic) Sally fought for authentic sunken-white data wells, Samus countered with the player's own note ('don't make it white'); truce = one sunken element, the milestone gauge. Cloud flagged the load-bearing trap: a naive ✕ would bypass the emergency modal's resolve-once seam and deadlock the paused sim, ✕ now routes through the cancel path.
- (outcome) Deep review of PR #91 (stats modal restyle): Blind Hunter + Edge Case Hunter + Copilot/Codex. Patched: cancelable synthetic cancel, ✕ appended after showModal (initial-focus steal would let Enter decline emergencies), debt renders red not money-green, NaN guard on milestone gauge, sticky modal title bar (top: -18px to cancel box padding, top: 0 pins 18px down and lets text peek above). Deferred: hover re-show after inspector ✕ on narrow hover windows; escapeAttr-for-text pattern. Blind Hunter's 'critical ✕ no-op' was a context artifact, openModal reassigns oncancel=closeModal every open.
- (outcome) Deferred-work PR #94: inspector ✕ latch + shared escapeHtml (found u.label rename XSS already live, the deferred entry's predicted regression). Blind Hunter proved the first-cut latch wrong on both axes (permanent on touch re-tap, spent by null-pick jitter); hardened to: survives null picks, spent by different pick / explicit tap / adoptSim. Edge Hunter independently confirmed the adoptSim stale-id hole (ids reissued via setNextId on undo/load) and verified all escaping sinks safe. Codex bot hit usage limits, in-session review carried the load.
- (moment) Design party reconvened (Samus, Sally, Caravaggio, Cloud, Link) on the player's ask: grander lobbies, the canon roof crane until floor 100, and exterior escape stairs. Caravaggio roasted the current lobby as 'a beige hallway with a gold sticker'; Sally spotted the cloned static patron baked into every 11px lobby tile.
- (outcome) Design decisions landed: lobby becomes a 4-tile repeating concourse (column/plain/centerpiece/plain) baked per lit x ground x variant, chandeliers+sconces glow after dark, sky lobbies get planters+framed prints; canon roof crane (animated trolley/girder/beacon) perched over the highest floor's run until GRID.maxFloor; fire-escape zigzags hung per-floor off the silhouette edges, parity-flipped. Cloud's constraint drove the whole design: lobbies are one shared 11px tile, so grandeur had to tile at x%4. Sally killed the cloned static patron baked into every tile.
- (outcome by adversary) Shipped PR #96 (lobbies/crane/escape stairs) + review-fix commit 31ff0e0. 8-angle in-session review confirmed 9 findings (best: Boundary's corrupt-save lobby-variant index crash; Grumbal's mid-air crane over a bulldozed lot; chandelier glow seams at tile edges). Codex bot hit usage limits AGAIN (twice running); Copilot's crane-smear claim refuted by reading Raster.rasterize (clearRect built in). Perf pass: facade sync gated on structural revisions, crane raster cache+flagDirty, guarded graphics.use.
- (moment) Copilot's re-review round 2: two real hostile-save geometry holes (unbounded lobby width loop, NaN floor/x into facade math), fixed at the deserialize seam Boundary built, with grid clamps + regression test (8da7618); its crane-latch complaint resolved toward derived state ('demolish below 100 = under construction again'). Copilot's earlier crane-smear claim stayed refuted. Boundary's seam keeps winning arguments years after it was poured.
- (outcome) PR #96 MERGED: grand lobbies (4-tile concourse, chandeliers glow at night, sky-lobby identity), roof crane until floor 100, zigzag escape stairs. Four Copilot rounds survived, every real finding fixed same-session, both bogus claims refuted with source reads. The room's new running bit: Copilot chasing our clamps to ever-finer boundaries until its last finding was literally a stale comment.
- (event) Design panel reconvened (Sally UX, Samus game-design, Cloud architect, + Link Freeman game-dev) on dialog/modal consistency: full inventory of every floating surface (modal x12 contents, editor, inspector, toasts, hint, saves slots, splash, coach marks)
- (outcome) Dialog standard ratified: one window grammar (gray face + bevel-out + navy full-bleed title bar + white X) for every dismissible surface; ONE button family with primary = Win-style dark default-button ring + bold (killed the flat web-blue .primary fill), danger = red bold label on same chrome; saves slots became a sunken white data well (File Manager idiom) with flat rows; sticky modal title bar magic number replaced by --pad-y/--pad-x tokens; 36px touch targets under pointer:coarse; editor/inspector h4 bars now full-bleed like modal h2; editor X fixed title-fg white (was muted-on-navy, illegible)
- (dynamic) Sally cashed in the stats-modal debt ('you owe me a white well') and got the saves slots as a sunken well; Samus won the splash exception (title screen = box art, keeps cream/amber palette, only interaction grammar shared) but conceded the amber primary never leaves the splash; Cloud named the root disease: two CSS generations fighting by specificity, ruled 'delete superseded rules, never stack a third generation'; Link enforced surgical diffs (no class renames, restyle via CSS only) and vetoed touching .pal-item hover as out of dialog scope
- (event) Architecture party on breaking up main.ts (1,446-line GameApp): Cloud, Winston, Link, Sally, Samus, with Grumbal/Boundary/John walk-ons.
- (outcome) Ratified module map: src/game/ controllers, buildActions (build/remove/refund/paint verbs, the money boundary), editorActions (handleEditAction + stops/batch/extend dialogs), saveLoad (save/load/import/new/recover; adoptSim STAYS in main as the injected world-swap primitive), inspector (pick + latch state), keyboardPlay (kb cursor/anchor + move/commit/announce-cursor). main.ts keeps: constructor wiring, tool+selection spine, update loop/HUD, wireEngine (ruled un-extractable wiring), bindKeys switch, adoptSim, undo delegators, boot. Extraction order: build -> editor -> saveLoad -> inspector -> keyboard.
- (outcome) Laws ratified: "sim is a guest, not a resident" (getSim() getters only, adoptSim reassigns this.sim; modules never capture it; keep the re-lookup-by-id idiom), narrow per-module constructors (no shared GameContext bag, Grumbal's god-object objection), no event bus (John's gold-plating veto), moves not rewrites (Link), announce routes through one throat with strings pinned by tests (Sally), paintFloorRun call frequency untouched (Samus's feel contract).
- (dynamic) Grumbal opened with 'god object with extra steps' and was disarmed by the two rules rather than argued down; John's 'who asked for this' was answered by Boundary naming handleEditAction's untested money paths; Boundary's seam bit continues ('sim is a guest' joins 'version:1 is a lie'). Grumbal claimed the extraction order as his win.
- (event) Tenant-churn party (PR #109): Samus, Cloud, Sally, Paige debate the vacating notice-period mechanic, is the 2-day/25% grace too forgiving, and should the missing gdd-/arch- doc pair be back-filled.
- (outcome) LANDED on #109, 'hurt them, but inform prior'. Rescind bar 0.25->0.40 (Samus: stabilized != fixed); 2-day window kept (Cloud veto on shortening = eviction-spam risk); 'inform prior' = live inspector countdown + current-vs-40% target (Sally's fix that bought Samus's buy-in). Back-filled gdd-/arch-tenant-churn-2026-07-03 with load-bearing invariants at the top of the arch doc (Paige+Cloud). 413 tests green, endgame+parity verified.
- (dynamic) Samus opened calling the numbers 'a hug'; Sally refused to let him amputate the grace window and reframed the fight as legibility; Cloud kept vetoing anything that reopened the toast-spam commit or shipped a constant unverified; Paige weaponized the argument itself as proof the doc had to exist. Running bit: 'the doc is the fix for the thing we were all afraid of.'
- (event) Condo-churn party (after shipping PR #118 per-unit satisfaction heatmap): Samus, Cloud, Sally, John, Paige debate whether sold condos should stay sticky like the 1994 original vs. churn with consequence. Trigger: player noticed sold condos vacate (office-noise erosion) and re-buy next day for $0 with no downside, rating only ratchets up, sold condos overhead-exempt, condos pay no recurring rent (one-time everOccupied-gated sale). NOT an infinite-money glitch (confirmed in code + save: all 168 condos occupied/everOccupied=true).
- (outcome) NO CODE CHANGED, parked as an open design decision. Room walked off 'numb-sticky' when the player asked the load-bearing question: if condos are sticky, what is satisfaction FOR? (Post-sale, condo satisfaction currently drives only the heatmap color + the walkout at 0, delete the walkout and the gauge we just polished in #118 becomes a mood ring.) Reframe (Samus): office = a lease you MANAGE; condo = a bet you PLACED. The bug is both run through the same vacate() path. Leading synthesis = 'patient flight risk': condos sticky against SPIKES (noise/one bad rush hour) but sustained rock-bottom neglect eventually loses the owner, longer/higher-tolerance fuse than the office 2-day hair-trigger. Money stays canon (untouched). Open fork left with the player: is condo satisfaction VALUE (sets sale price/resale/reputation) or a slow FLIGHT RISK? Cloud's rating-pressure option deferred, it cracks the 'should star rating fall?' can nobody wants to open (evaluateStar only ratchets up).
- (dynamic) Callback to the #109 tenant-churn party ('hurt them, but inform prior'): Samus opened vindicated that offices got a consequence engine + countdown while condos got nothing. Cloud's fidelity argument (1994 condos were sticky because residents were never stress-modeled like offices, we accidentally ran NOISE_EROSION on condos at office intensity) kept winning until the PLAYER, not the room, found the crack: sticky guts satisfaction. Paige's running bit held ('the doc is the fix for the thing we were afraid of', rewrote the condo invariant twice in one session). John repeatedly vetoed inventing a resale-fee economy as gold-plating. Player ended 'not sure' then 'ok', room read it as done, refused to force a verdict; Sally: 'not sure isn't a gap, it's data.'
- (outcome) Traffic chip 'always Smooth' bug: room found the chip (Smooth/Busy/Backed up/Gridlock) is a MODERN a11y invention, not 1994 canon. Canon traffic signals (red walkers, congestion heatmap, utilization stats) are ALL spatial/worst-spot. Killer: peakCongestion() already exists and the overlay LEGEND uses it, while the chip uses the tower-wide AVERAGE, two HUD readouts disagreeing, hidden because the average is always green. Consensus: repoint chip to peakCongestion() (parity + fixes internal contradiction), derive tier boundaries from measured towers, blend max(avg,0.7*peak) reserved as documented modern softening only if peak proves twitchy. Hard line (Cloud): do NOT touch stress=clamp(congestion-1) economy math, repoint chip + rewrite lying 'shape and color agree' comment only.
- (outcome) Traffic chip, NUMBERS pressure-tested against 7 real saves + starved/lopsided/hotspot probes. Corrected earlier premises: empty floors do NOT dilute the avg (mean is over populated+served floors only); under-provisioning STRANDS floors (unreachable, drop out of calc) rather than congesting them, so peak barely moves (0.29->0.41 pulling 23 shafts). avg vs peak only sharply diverge on a localized jam among healthy floors (avg 1.15 vs peak 3.11), there peak is the true signal (20x dynamic range vs 5x for avg). All 7 real saves peak 0.17-0.29 => user's 'Smooth' screenshot is CORRECT; real defect is zero sensitivity in the 0.15-0.4 band. CONVERGED: signal=peakCongestion(); thresholds Smooth<0.4 / Busy 0.4-0.8 / Backed up 0.8-1.5 / Gridlock>1.5 (measured, not vibed); coupling DECOUPLED (chip=early warning at 0.8, red walkers=acute at congestion>1.0 via untouched stress gate); PARITY=peak+thresholds mode-agnostic; MODERN='what 1994 couldn't'=chip names worst FLOOR ('Backed up 42F'), Sally's long-owed HUD hotspot well; BENCHED=blend max(avg,0.7peak) gated on playtest; FOLLOWUP=tap-to-jump-to-hotspot (Cloud scoped out, touches camera); KNOWN GAP=peak==0 when nothing reachable (reachability's job).
- (outcome) gds-code-review ran (3 adversarial layers). Blind Hunter (diff-only) flagged a CONFIRMED basement bug: floor>0 gate drops basement hotspots + 0-sentinel collides with B1. Edge Case Hunter (repo access) REFUTED it: only office/condo/hotels have population>0 and those are exactly NO_BASEMENT_KINDS (daylight-only), so the congestion map never contains a floor<=0, bug unreachable. Adjudicated in code (facilities populations + Tower NO_BASEMENT_KINDS): Edge Case Hunter right. Blind-vs-informed split earned its keep. PATCHED anyway (lean hardening): peakCongestionFloor()->number|null (kills 0-sentinel ambiguity, documents the daylight invariant); single-sourced TRAFFIC_BOUNDS shared by trafficTier + main.ts hysteresis (kills dual-maintenance hazard); added CAP-1 shared-signal test. DEFERRED to backlog: hotspot-floor aria-live jitter on near-tie crossings (a11y polish), pre-existing multi-tier-jump 0.02 early-entry. Gates green: typecheck/lint/540 tests/build. v1.3.0.
- (outcome) Traffic chip layout bug (user phone screenshot): the '· 11F' modern readout wrapped the fixed HUD cell to 2 lines, colliding with the glyph and breaking the bar's vertical centering ('weird bottom spacing'). Sally owned it ('I shipped it without checking the box it lives in'). Fix: #traffic white-space:nowrap; split the floor into its own #traffic-floor span styled as a smaller/lighter footnote (0.82em/500, inherits red warn) so long strings like 'Backed up · 100F' never wrap or dominate; blank the suffix at Smooth (no orphan '·'); full sentence stays in aria-label. Verified desktop + mobile (mobile #status flex-wraps whole cells to a 2nd row). Boundary flagged max-string + empty-case; Samus vetoed ghost-gray (kept same color). Mode-agnostic, engine untouched.
- (event) Save-perf party (PR #156 takeover from Copilot): Cloud, Link, Indie, Grumbal, John walk-on. Question: faster than JSON.stringify for autosave, benchmarked on the player's real 12,975-unit towerone_6.vctower.
- (outcome) VERDICT sparse v3 units: serialize() omits loader-default fields (state/satisfaction/occupants/everOccupied/pendingIncome, catalog label), JSON drops to 33% (2.09MB->692KB), zero round-trip mismatches against Boundary's deserialize fallbacks. Sync writer drops to deflate level 1 (sparse makes L1 cost only 0.8% size for 3x speed): pre-reload flush 48ms->16ms, stored slot 132KB->116KB. Hand-rolled stringifier REJECTED by measurement (10.5ms vs stringify 7.1ms, the bytes are the bottleneck not V8). Binary codec stays out of scope per Copilot's decision log; native toBase64 to backlog (John's gold-plating veto).
- (dynamic by adversary) Grumbal's kill shot earned the width carve-out: omitting width-at-catalog-default would silently re-lay rooms when tuning widths drift (the v1->v2 reflow exists BECAUSE widths drifted, fastFood 12->16). Ruling: width omitted only for width-1 floor/lobby tiles (the grid unit itself); rooms always write width. Boundary's seam wins again in absentia: sparse saves only work because deserialize already defaults every omitted field.
- (event) Repo-topology party (mobile distribution, PR #159): Cloud, Winston, Link, John, Grumbal walk-on. Question from the owner: should the private distribution repo be a full-history mirror kept in sync, a submodule shell, or the arch doc's consumer repo?
- (outcome) CONVERGED fast (Grumbal: 'suspicious'): consumer repo + tracked one-line PUBLIC_REF file (tag/SHA the iOS build pins to). Submodule's only real advantage (declarative, reviewable pin) captured by the file with none of the recursive-checkout tax; mirror rejected for sync toil + Grumbal's two-remote leak hazard (same commits, one private and one public remote, wrong-push risk defeats the separation the repo exists for). Mirror re-asked exactly once, at the E4 monetization gate, if private game-code changes are chosen (MIT keeps that door open; creating a mirror then is minutes). Key reframe (Cloud): 'in sync all the time' already holds structurally: TWA renders the live site (Android always current by construction), iOS pinned on purpose (reproducible store snapshots), so continuous repo sync serves no job. Ratified into arch §1 + PRD decision-log entry 12 on the PR branch.
- (dynamic) First party in memory to converge without Grumbal breaking something; he claimed the leak-hazard point as his contribution and told the room to ship before he finds the flaw. Winston steelmanned both losing options and got to keep the 'mirror at E4 if private code' escape hatch. John's gold-plating veto ('machinery serving what job?') did its usual work. Link coined the closer: 'you know what also pins declaratively? A file.'
- (outcome) PR #158 MERGED (v1.10.1): sparse v3 units + level-1 sync deflate + async autosave shipped. Survived the in-session 3-layer review plus SEVEN Copilot rounds; every real finding fixed same-session (per-direction compression probes, quota retry + legacy-key restore, loadResult legacy fallback, slot-save token isolation), the memoize-the-probes suggestion refuted (would break the stub-based fallback tests for microseconds). Player's real tower: JSON 2.09MB->692KB, sync flush 48ms->16ms, stored slot 132KB->116KB. Main moved three times underneath (1.9.4, 1.9.5, 1.10.0 importer); rebased past each. Copilot's #156 closed as superseded. Grumbal's width carve-out and Boundary's fallback table both load-bearing in the shipped code.
- (event) Modern-mode roundtable (Samus, Cloud, John, Sally; Paige doorway walk-on) after Classic locked parity (importer+exporter shipped): what is Modern FOR?
- (outcome) Prioritized Modern shortlist: 1) condo "patient flight risk" (resolves the parked condo-churn fork; Classic keeps 1994-sticky; rule-site mode gate + econConfig modern keys) 2) stars can fall, bounded (grace window, only un-earned stars, never TOWER; epic-sized, needs gdd-/arch- docs, Cloud still holding the ratchet-can lid) 3) named tenants (importer people records finally earn their bytes; feeds 1+2) 4) post-TOWER prestige parked until demand. RECLASSIFIED out of Modern: elevator schedule blocks + finance window = Classic parity gaps (1994 HAD them, the 56-byte TDT schedule block is the receipt); tap-chip-to-hotspot = mode-agnostic UI (Sally still owed it). Sequencing ruled: Classic queue first (exporter review triage, deserialize-coercion P2, walkway canon check). Every Modern rule widens the exporter's refuses-Modern gap, named as a standing cost per proposal (Cloud).
- (dynamic) John's "don't let 'we don't have it yet' masquerade as 'the original couldn't'" reframed two pitches mid-flight; Samus finally said "stars can fall" out loud after circling it since the traffic party; Paige's running bit fired from the doorway ("the doc is the fix for the thing you're all afraid of"); Sally tried to ride the hotspot jump into the Modern lane and Cloud rerouted it to mode-agnostic UI instead of vetoing.
- (outcome) Epic-review party (Classic/Modern roadmap epics doc): 3-epic structure ratified unchanged (pricing split / condo flight risk / scheduling spec-first). Two doc changes: FR4 sharpened to No Rate = OFF-MARKET (charges nothing AND blocks move-ins, one inseparable state; Grumbal's import case = class-4 units must not flood with zero-rent tenants), NFR3 rewritten to SNAP-ON-LOAD (owner call: pre-split Classic rents snap once to nearest rung at load; one-time income shift accepted as noise under Decision 2's 10x hotel rescale; golden fixture pins the migration). Sally's labeled grandfather row considered and rejected as a modern smudge on a canon dropdown.
- (dynamic) Cloud+Grumbal tag-team caught No Rate as a tenancy state machine smuggled inside a UI epic ('wearing a pricing costume'); John reframed NFR3 as 'a support ticket wearing a UX diploma' and forced the snap-vs-grandfather fork to the owner; Samus defended the 'Condos you can lose' framing against template softening; Grumbal's museum-exhibit jab at spec-first was answered by the owner tiebreak (spec decides how, not whether). Paige's doorway bit fired again.
- (outcome) Elicitation-findings party (GDS crew) on the roadmap epics doc: all five-lens findings ratified with 3 amendments. Link's save of the condo edge = the room's best move: the fire-sale condo snapping 80k->100k looked like gaslighting until he showed canon's 50k floor UNLOCKS a faster fire-sale than the old 80k clamp ever allowed; migration note now sells it as a feature callout. Cloud enforced uniform nearest-ties-up (no intent-guessing migrations) and noRate coercion as a GameRules method; engine-state-first became story NUMBERING. Grumbal's occupied-No-Rate census challenge (free tenants still climb the star ladder) answered by Samus as canon (endpoint of the 1994 cheap-rent lever) and pinned in the AC so nobody fixes it.
- (outcome) Epic 2 fuse party: ruled the flight-risk fuse is an ACCUMULATOR with decay (never a steeper erosion rate, 'erosion rates are how we got the office hair-trigger'), riding the satisfaction pass with econConfig modern keys. Grumbal called the $0 resale 'the exploit with extra steps'; Samus flipped it: $0 IS the sting (paid once ever, hold tax while empty, population gone) and pinned it in an AC so nobody generously pays twice. Cloud's determinism veto stood (resale rerolls household from seeded RNG). Epics doc COMPLETE: 3 epics, 11 stories, all 14 FRs + 5 UX-DRs covered, validation green.
- (outcome) Sim-split party (game+dev): owner asked if ModernSimulation should extend ClassicSimulation. Winston steelmanned it, room killed it 3 ways: Boundary's construction-order attack (polymorphic deserialize lets a forged save choose its attack surface before validation), Grumbal's silent-bugfix-inheritance page, Samus's 'modes disagree, they don't stack' (wrong verb for inheritance). Evidence-first: grep showed ZERO mode branches in sim logic today, the seam holds. Verdict: strategy seam IS the split; shipped Story 1.7 tripwire test (suite fails on mode branches outside gameRules.ts) so the law enforces itself; grouping threshold (~12 methods -> rules.economy.* style) noted, never subclassing.
- (outcome) Owner tiebreak 2026-07-08 (PR #164): the Modern-mode roundtable had RECLASSIFIED elevator schedule blocks as a Classic parity gap (1994 shipped them, the per-shaft TDT schedule block is the receipt), while the pricing/roadmap GDD still listed elevator per-day-type scheduling under "do NOT build". Conflict surfaced in review. Owner ruled FULL PARITY: build the scheduling behavior AND its UI to match 1994, and round-trip the TDT schedule bytes on import/export. Recorded in gdd-classic-modern-pricing-roadmap-2026-07-08 (moved out of "do NOT build" into a spec-first tier-2 roadmap row, §7 rewritten as resolved) and in backlog `elevator-scheduling` (epic, spec-first; needs its own gdd-/arch- docs covering the schedule model, the editor UI, and how a user schedule composes with our automatic SCAN dispatch). The 3-day-week/12-day-calendar item was never part of the conflict and stays ratified-out.
- (outcome) Approval party (game+dev bench, PR #165 epics doc): UNANIMOUS six-for-six (Samus/Cloud/Link/Sally/Boundary/Grumbal). Cloud's on-record note: Epic 1 is 7 stories against one PR, engine-state-first ordering is what makes it reviewable, hold it like law. Sally re-filed her receipt: the traffic-party hotspot well is still in nobody's epic. Grumbal's approval verbatim: 'the doc had already written down the thing I was going to yell about, with a date and who ruled it. Ship it before I find something.' PR opened for review (out of draft) on the owner's word.
- (event) Condo household-departures party (2026-07-08, tier-1 #3 of the Classic/Modern roadmap): Samus, Cloud, Winston. Owner sent the economic-model decision to the party with the steer "keep Classic as parity-faithful as we can." Question: when a Modern condo household relocates (a life event, not neglect), what happens economically? A = reuse the existing full-price buy-back (reclaim at householdPrice(residents), unit re-sells fresh); B = free turnover (no reclaim charge, unit re-sells booking fresh income); C = something else.
- (outcome) UNANIMOUS A (reuse the buy-back). Samus: B is a churn-farming faucet (each relocation books a fresh sale = free money), a per-reason discount just forks the shared vacate() lifecycle for a cost the math mostly erases; the copy must read as relocation, never a complaint. Cloud: ride the single shared vacate() spine untouched (relocation is a sold everOccupied condo, so zero new code in the invariant-dense method); B forces a reason-fork of the one seam we want un-forked; the ONLY real hazard is RNG-stream perturbation, solved by short-circuiting on p>0 BEFORE the draw (the pattern sellCondo already uses) so Classic (returns 0) stays byte-identical; rate constant in an econConfig Modern key (condoRelocationChanceMonthly), Classic returns literal 0. Winston's correction (load-bearing): A is NOT net-zero, it is a mild SELF-SCALING SINK: relocation risk scales UP with family size, so you systematically buy back 4s/5s at householdPrice(residents) while every re-sale regresses toward the mean 3 (net drain asking/3 * (r_new - r_old)), AND the bought-back unit bleeds condoMonthlyTaxRate + overheadPerLeasableUnitMonthly while it sits unsold. That drain IS the intended anti-trivialization the Modern layer exists for; B re-introduces the exact success-scaling free-money disease those sinks were added to cure. Parity is NEUTRAL between A and B (Classic never rolls, RNG byte-identical); the real axis is "don't deepen the Modern divergence", and A adds ZERO new economic semantics (the already-ratified buy-back, unchanged). Notice model settled by the owner separately: informational, non-rescindable advance notice (a life event can't be fixed by improving the tower). Save/reload double-charge checked SAFE (everOccupied cleared in vacate, idempotent). Pre-existing (not new, do not block): the re-priced-condo buy-back arbitrage and the broke-player-goes-negative both already exist on the neglect buy-back path; relocation reuses it verbatim. Build: new GameRules.condoRelocationChance(residents) (Modern scales up with household, Classic 0), monthly roll in payMaintenance short-circuited before the RNG draw, new non-rescinding "relocation" VacateReason + copy, minor version bump.
- (event) SimTower retail ISO arrived (owner's Downloads: /mnt/c/Users/nafta/Downloads/SimTower.iso, 36.5MB, label SIMTOWER). Owner asked for a "VM that loads it properly" to test vctower<->tdt round-trips against the REAL 1994 game, plus read the on-disc manual for parity. Chose Wine-in-Docker over a full Win98 QEMU VM (owner confirmed): no Windows license, reproducible, fits the repo's Docker habit, and the game's C: drive is a host-mounted folder so real .TDT saves flow straight into tests. Host facts: Docker 29.5.3, /dev/kvm + WSLg present, NO passwordless sudo (so apt lives inside the container). Built tools/simtower/, extract-cd.py (pycdlib pulls the KWAJ-compressed game files + PDFs off the ISO), docker/kwajd.c (libmspack one-shot KWAJ decompressor; Wine's expand can't do KWAJ-LZH), docker/entrypoint.sh (decompress -> C:\SIMTOWER, run/screenshot/load modes), run.sh (WSLg GUI wiring; runs as host uid + HOME=/wine so Wine accepts the bind-mounted prefix).
- (outcome) Binary forensics corrected my own wrong call mid-stream: I first read STOWER.EXE as a 32-bit PE and announced "SimTower is Win95 32-bit", but STOWER.EXE is a Macromedia Director autorun shell, NOT the game. The real simtower.exe (KWAJ-decompressed to exactly 6,566,400 bytes as setup.inf predicted) is a 16-bit Windows 3.1 NE that renders through 16-bit WING.DLL. Corrected the Dockerfile/extract comments. Debian's Wine runs the 16-bit binary fine: splash + New/Load/Quit menu render correctly headless (Xvfb capture; xdotool auto-clicks past the "no sound card, run without sound?" dialog).
- (outcome) MANUAL parity: the disc's four manuals are thin for our purposes, only the Italian PDF has a text layer and it's a tutorial narrative (inline "30.000 dollari" offices, not spec tables); French/Spanish are 5-page quick-refs; the full 70-page German manual is SCANNED images (needs OCR to mine the real data tables). Parked OCR as optional if we want the complete numeric reference.
- (outcome) vctower->tdt ROUND-TRIP proven against the real game (owner supplied towerone_5/_6.vctower). vctower-to-tdt.mjs decodes the .vctower container (fflate) and runs the engine's OWN buildTDT via Vite SSR (so it can't drift from ship). Real SimTower LOADS our exported TOWER5/6.TDT with correct headline state: 3★, Pop 1280, Funds $11,020,800 / $9,126,300 (= the vctower money through the TDT's ÷100 storage quantization, losing $8/$35), date 107th/108th Year. Structure independently verified by verify-tdt.py, a canon-spec parser deliberately NOT sharing our importer, so it catches bugs both sides agree on: magic 0x2400 OK, floor map walks CLEAN, unit-type tally matches the tower (office/condo/hotels/shops/parking; multi-story cinema+recycling+partyHall correctly stored as merged top/bottom halves).
- (outcome, CONFIRMED BUG, resolves a recorded risk) The exporter's own comment "the undocumented region is zero-filled; whether the real game needs anything there is the recorded real-game validation risk", it DOES need it. buildTDT writes magic/star/balance/tick/day then pad()-zero-fills the rest of the header, leaving recyclingCount / commercialCount / securityCount / parkingStallCount / hallCinemaCount / lobbyHeight all = 0. The real game TRUSTS them: unpatched TOWER5 loads showing a FALSE advisory "Your tower needs a Recycling Center" despite owning two. Byte-patching the six fields to correct values (recycling 2, commercial 15 = shops8+rest3+ff4, security 1, parking 85, hall+cinema 4, lobbyHeight 1) and reloading makes the false advisory VANISH, decisive proof. FIX = populate these header aggregates in buildTDT from tower contents (+ a verify-tdt / real-game regression). SEPARATE still-open issue: winevdm.exe crashes ~12-16s after load, but reproduces on the PATCHED file too, so it is NOT the header counts, a Wine 16-bit fragility (or other data edge) to chase on its own. Minor unit-level anomaly noted for follow-up: one stray "burned" (type 48) unit and hotelSingle 181 vs source 182.
- (event) Party convened on two linked decisions from the SimTower-ISO session: (Q1) commit the Wine harness for replication? (Q2) how to land the confirmed header-count fix? Room: Cloud (architect), John (PM), Boundary (seam/clean-room), Link (game-dev), Paige (tech-writer), Grumbal (adversary). [session mode; roster resolver blocked by py3.11/uv, improvised from history.]
- (outcome Q1, COMMIT THE HARNESS) Ratified: commit tools/simtower/ into THIS repo (not a separate repo, Link/John killed that per the #159 topology precedent: dev tooling belongs next to the canon doc it validates, a split just lets harness+spec drift). Clean-room CLEARED by Boundary's reframe, which won the room: the policy line is game BYTES, not plumbing, kwajd.c is a generic libmspack KWAJ wrapper (zero game IP) and verify-tdt.py is just docs/canon/tdt-format.md in executable form (and that doc, already committed, is far more detailed than any tool). ISO + extracted binaries stay gitignored. Opt-in / bring-your-own-ISO; NEVER wired into required CI (Link: an ISO-gated CI check is red for everyone forever). Paige owes a one-page README carrying the clean-room rationale in Boundary's words. GRUMBAL'S KILL-SHOT THAT STUCK: the CI regression fixture must be OUR OWN OUTPUT (a TDT buildTDT makes, ideally generated at test time and never committed), committing one of the owner's real save files would smuggle a game-adjacent artifact into the clean repo. New hard rule: "our bytes always, their bytes never." Grumbal's separate-repo motion died; his fixture catch lives.
- (outcome Q2, STRAIGHT buildTDT PATCH, no epic) Ratified: it's a bugfix completing a stubbed pad()-write, not a feature (Link named it, John blocked any gdd/arch doc). Derive all six header aggregates (recyclingCount, commercialCount=shops+restaurants+fastFood, securityCount, parkingStallCount, hallCinemaCount=partyHall+cinema, lobbyHeight), but the LOAD-BEARING insight (Boundary, Cloud seconded): count from the EMITTED TENANT RECORDS, not the input rooms. The original bug was a header disconnected from the map; counting the input (which buildTDT already drops for out-of-range rooms) rebuilds the same disconnect one level up, just rarer. Clamp to canon caps (securityCount max 10, Grumbal's forged-save angle: buildTDT "takes any serialized input"); pull REAL lobbyHeight 1-3 from the sim, not the placeholder 1 the byte-patch guessed. RECORD (Paige): invariant note in tdt-format.md §1 (fields already listed there) + a PARITY.md line, "the 1994 game trusts these header aggregates; derive from emitted floor-map contents, clamp to canon caps." GUARD: a vitest unit test that asserts header-fields == recomputed-from-floor-map (encode the INVARIANT, not a hand-typed "expect 15" that rots, Boundary); verify-tdt.py committed as the byte-level cross-check. OUT OF SCOPE -> backlog: the winevdm crash (Link's control experiment held against Grumbal, reproduces on the PATCHED file, so not our data) and the one-unit export drift (stray type-48 'burned' + hotelSingle 181-vs-182, Grumbal's "don't call export clean until someone explains the drift").
- (dynamic) Boundary's seam bit struck twice in one session, "the line is the bytes, not the plumbing" cleared the clean-room fear on Q1, then "count the emitted tenants, not the input" caught that the naive fix rebuilds the very disconnect the bug came from. Grumbal lost the war (harness ships, patch is small) but won two real carve-outs by playing pure adversary: the fixture-must-be-our-output rule and the securityCount/lobbyHeight hardening. John's gold-plating veto did its usual work (no doc, no CI gate, no second repo). Running bit intact: "the doc is the fix for the thing we were afraid of", Paige turning the clean-room anxiety into a committed README.
- (outcome) SHIPPED the export header-count fix (v1.11.3): buildTDT now derives recyclingCount/commercialCount(=shops+restaurants+fastFood)/securityCount/parkingStallCount/hallCinemaCount + lobbyHeight from the EMITTED rooms and backfills them at their doc-§1 offsets (securityCount clamped to canon max 10; parking/commercial to 512). Guarded by the party's ratified vitest test (asserts the six counts from a hand-built tower, proving burned type-48 shells AND out-of-range rooms are excluded, plus the security-cap) + the committed independent verify-tdt.py. Gates green: typecheck/lint/901 tests/build. Harness (tools/simtower/) committed with Paige's clean-room README. verify-tdt.py confirms TOWER5/6 now carry the right counts (was all-zero).
- (outcome) Wine reality check: upgraded the harness from Debian's Wine 8.0 to WineHQ **staging 11.10** + a Wine virtual desktop (rootless WSLg wouldn't route clicks). Result: the game now PLAYS interactively (owner built a rainy 4-floor tower, is spotting parity gaps live), big win for VISUAL parity. BUT winevdm STILL crashes on SimTower's 16-bit file Open/Save dialog across BOTH Wine versions, so interactive save/load (the path to real .TDT saves for importer testing + the canon byte-diff) is a Wine dead end. NEXT: stand up DOSBox-X + Win3.11 (or a Win98 QEMU VM) for reliable save generation, the "proper VM" the owner originally floated. The winevdm crash line in the earlier entry is now explained (16-bit commdlg, Wine-version-independent), not our TDT data.
- (event) Owner parity observations from playing the real game (harness), logged to backlog for future parties: (1) 1994 auto-builds the floor behind an elevator extended past the structure, and auto-fills floors in the gap between two modules, our Classic forces manual floor-building (backlog `auto-floor-build`, QOL-vs-parity party call pending); (2) earlier: overview MAP window, parallax background cityscape, and pictographic facility icons (backlog `overview-map` / `background-cityscape` / `facility-icons`).
- (outcome by adversary) Ran the ratified /bmad-code-review (3 layers) on the v1.11.3 header-count fix. Blind Hunter's lone "High" (header could over-count the floor map) was REFUTED by the Edge Case Hunter with repo access: every counted kind provably emits, single-story counted kinds all have KIND_TENANT ids, multi-story recycling/cinema/partyHall use the plain unconditional-emit branch (only weddingHall's collision branch can drop parts, and it isn't counted), and fitsTdtRows already tests the span top. Overflow refuted too (total tenants <= 256*120 = 30,720 < u16, and export throws on a floor overflow). Acceptance Auditor confirmed all six offsets/groupings/clamps match canon §1/§7/§10 and the emitted-not-input invariant is sound + well-tested. NO confirmed bugs. Patched 3 cheap nits: a gutted/burning floor-1 lobby no longer sets lobbyHeight=1 (extends the "a burned shell never inflates a header" invariant from the counts to lobbyHeight); clarified the commercial-512 ("one unit per slot") and recycling/hallCinema ("no canon cap") clamp comments; added a tripwire test asserting every counted kind emits a tenant (guards the switch-vs-emit-table drift the Edge Hunter flagged). DEFERRED to backlog `tdt-header-counts-verify`: (1) commercialCount vs the still-empty §7 retail table, (2) whether the real game credits under-construction facilities, both need the real-game harness to settle. Gates green (typecheck/lint/28 tdt tests). Running bit: the informed layer refuting the blind layer's scariest finding earned its keep again (callback to the traffic-chip basement-bug review).
- (outcome, marathon) Got the real game to SAVE and cracked open real-save validation. Journey: Wine plays great but its BUILTIN 16-bit commdlg crashes SimTower's file Open/Save dialog (page fault 0x048B). Fix = load the genuine Microsoft 16-bit COMMDLG.DLL (from the owner's Win3.1 disks) as native -- the load-order key is exactly `commdlg.dll16=n` (plain `commdlg=n` does NOT match Wine's 16-bit loader; found via WINEDEBUG=+loaddll showing "builtin" vs "native"). With that, in-game Save works and writes real .TDT to the host saves/. Detour that got REMOVED: a full DOSBox path (plain DOSBox Win3.1 install, then a DOSBox-X + pre-installed Win95 image via the idos archive, SimTower injected by loop-mount) -- Win95 booted 256-color with save/load working, but the captured relative mouse on the owner's high-DPI ultrawide was unusable (endless sensitivity tuning; asymmetric X/Y from window scaling). Once Wine's Save was fixed, all the dosbox/ tooling + images were deleted; Wine won (native window = absolute pointer).
- (outcome, the payoff) REAL SAVE captured (TSTSAVE.TDT, empty New Tower, 65,112 B) and mined. (1) Our IMPORTER parses it CLEANLY -- classic/1star/$2,000,000/0 warnings -- and our independent verify-tdt.py confirms the money scale (x100), the 194-byte/floor map layout (560 + 120x194 = 23,840), and lobbyHeight=0 for an empty tower (matching the exporter's new logic). (2) DECISIVE experiment: the real game LOADS ITS OWN save fine but CRASHES loading OUR export (page fault 0x0799) -> the crash is OUR DATA, not Wine. (3) Root cause by byte-diff: our empty export is 39,514 B vs the game's 65,112 B -- ~25.6 KB short after the floor map (our ~15.7 KB trailing tables vs the game's ~41.3 KB). The game reads its fixed-size trailing tables, runs past our too-small file, and overruns -> 0x0799. Filed backlog `tdt-export-trailing-structures` (P2/bug): emit every trailing table at the game's expected size (zero-fill ok, SIZE must match), using TSTSAVE.TDT as the byte-target; §11 lobby/reachability table (528x6) looks entirely unemitted. Header + floor map are correct (right star/funds/date/units shown on our files); only the trailing block is wrong. New reusable tooling: tdt-to-vctower.mjs (parseTDT + --diff-empty). The harness finally did its job: caught a real, game-crashing export bug and handed us the exact ground-truth bytes to fix it against.
- (outcome, RE marathon 2026-07-08 PM) Owner played a real tower to varied transport and saved (my_tower.TDT, ~88 KB: 3 elevator shafts [2 standard + 1 SERVICE] + 6 stairs + offices/condos/hotels/fast food). Used it to fix the IMPORT half of the trailing-structure epic (was: import silently synthesized 2 fake elevators, 0 stairs). THREE root causes cracked by byte-level RE against the real save: (1) ELEVATOR RECORD STRIDE -- the fixed appended block per built shaft is 3,140 B, not the doc's 480+2*120=720; the 720 figure desynced the table after shaft 1 and forced synthesis. Full stride = 194 header + 3140 + 324*servicedFloors + 348*cars; it reproduces all 3 shafts' exact offsets. Fix: `TDT_ELEVATOR_BUILT_FIXED` 720->3140. (2) KIND SIGNAL -- the header's `type` byte @1 is authoritative (0 express/1 standard/2 service), NOT the "capacity" byte @2: a real SERVICE shaft carried capacity byte 21 (the standard value, not service's 10). I first derived kind from capacity, imported the service shaft as standard, owner caught it live ("the right elevator was supposed to be a service elevator") -> reverted to `type`. (3) STAIRS TABLE -- located by SIGNATURE SCAN, not by summing finance(132)+parking(1026): the real stairs table sat 436 B BEFORE that summed offset (our parking/lobby block sizes are not pinned across saves), and the game packs built flights into the HIGH slots (~58-63 of 64), so a "first window with a flight" scan clipped 3 of 6. Fix = `locateStairs` scans from the elevator-table end for the 64-record window holding the MOST valid flights. RESULT verified live: 3 elevators (correct x 161/184/207, correct service kind, 1 car each -- the earlier "8 cars" was a synthesis artifact) + all 6 stairs import. Files: src/storage/tdtFormat.ts (stride const, capacity field + caveat, locateStairs, TDT_STAIR_SCAN_WINDOW), src/storage/tdtImport.ts (kind from type). Canon doc §8 updated. NOT yet gates/review-cleared or committed.
- (outcome) CALENDAR fully decoded + validated. Header currentDay=56 -> "5th Year / 3Q / weekend", confirmed by the real Finance Window showing "Year 5, Quarter 3". SimTower calendar (canon doc §3): week = 3 days (2 weekday + 1 weekend), quarter = 1 week = 3 days, year = 4 quarters = 12 days; day counter rolls at 11,987 (999 years). Our Clock uses a 7-day week / 90-day quarter / 360-day year -- a DELIBERATE divergence (PARITY.md), so imported dates render as "1st Year". OWNER CALL: make it a FAST-FOLLOW right after the export fix (it is TDT-coupled) and ahead of the population work -- Classic adopts the 12-day canon calendar, Modern gets a startup toggle (classic-short vs real-world). Backlog `classic-calendar-parity` (P1, epic, spec-first; scope lever = decouple cheap date DISPLAY from risky quarterly-rent PACING). Supersedes the "3-day-week ratified-out" note on the elevator-scheduling row.
- (outcome) POPULATION 2x gap (our ~119 vs real ~234) SOLVED by the owner's real Finance Window population column: Office 48 (=8*6, matches our `population` 6), Condo 45 (=15*3, matches our 3), FAST FOOD 175 (we count fast food as 0). So per-unit office/condo values are CORRECT; the gap is that the original counts COMMERCIAL-VENUE CUSTOMERS toward population (fast food ~35/unit; restaurants/shops presumably similar). Party (game designer + sim/economy architect + persistence architect, all 3 returned) converged: (a) our totalPopulation reads residentCount over isPresent, so offices/condos already count year-round and only hotels are time-gated (pop oscillates ~93 day/119 night) -- add a pure `Tower.associatedPopulation()` census for the POP display + star gating, keep totalPopulation/isPresent for the live crowd; (b) add canon commercial customer population; gate per-unit changes behind GameMode + a save-version gate. Star thresholds (300/1k/5k/10k/15k) + all facility caps + minStar gating CONFIRMED canon by the owner's in-game Help pages. Backlog `population-census-parity` (P2, sequenced after the calendar). 1:1 PERSON IMPORT: party ruled DEFER (unanimous) -- the §6 roster is a live snapshot our crowd overwrites within sim-minutes (~zero durable payoff); build the lighter "seed occupants from imported occupancy" instead (offices currently import with occupants:0; only hotels set it). Backlog `tdt-import-population-seed`.
- (outcome) EXPORT half still open ("File is already open, or damaged" loading our export in the real game). RE of TSTSAVE (empty save, 65,112 B) full trailing map: even an empty tower has a PEOPLE block of 512 records (16 B each), retail 512*18, elevators 24*194 (empty), finance 132, then a large region to EOF including the §11 lobby/reachability table (528*6 = a 6-byte-stride run starting ~0xca96) and ~9,840 trailing zero bytes; last structured byte ~0xd7e7. Our empty export is 39,514 B vs 65,112 (25.6 KB short): we emit people count=0 (-8 KB) and omit ~17 KB of post-finance trailing structure. Export fix = emit every trailing block at the real size (zero-fill ok, SIZE must match) INCLUDING full elevator payloads for built shafts (194+3140+...), using TSTSAVE as the byte-target. Backlog `tdt-trailing-structure-layout` / `tdt-export-trailing-structures`.
- (event) Owner catalogued more parity gaps while playing (all to backlog): per-room EVAL is computed differently and the 1994 inspector gives textual REASONING ("Single Room neighbor is noisy") we lack; the TERRORIST BLACKMAIL / bomb-threat random event ($200k demand or "Find the Bomb") we don't have; elevator config UI (WD/WE schedule strip, waiting-car-response, floor-departure, per-floor SHOW list, starting floor); stairs render poorly ("didn't even see the staircase" -- want top-floor overlap + shadowing); elevator floor-number labels unreadable on desktop (fine on mobile); the Finance Window UI itself (feeds `finance-1010`).
- (event) Owner parity observation 2026-07-08 (importing the real mobile tower towerone_6 via the harness): SKY-LOBBY / LOBBY HEIGHTS differ from ours. The real 1994 game gives ground AND sky lobbies (floors 15/30/45/...) buildable 1-3 story heights (TDT header lobbyHeight byte, docs/canon/tdt-format.md sec 1); our engine models every lobby as single-story (facilities.ts; Tower.ts "ground + every 15th"). Consequence for the TDT round-trip we are fixing: importing a real tower with 2-3 story sky/ground lobbies LOSES that height (they come in single-story), and buildTDT writes lobbyHeight 0/1 only. So the mobile-save round-trip cannot be fully faithful until lobby height is modeled. Recorded on backlog `lobby-height` (P2, extended from ground-only to sky lobbies + the TDT-fidelity angle): resolve in Classic first (canon 1-3 story ground + sky lobbies, round-trip the height on import/export), optionally richer in Modern. Owner asked to keep this in the memlog through compaction.
- (outcome) CONFIRMED the sky/ground lobby-height mechanic (SimTower Wiki, owner 2026-07-08), correcting my earlier hedge: multi-story lobbies ARE real 1994 canon. They are a HIDDEN input: hold Ctrl (2-level lobby) or Shift+Ctrl (3-level lobby) when FIRST placing a lobby (works for ground AND sky lobbies, so my sky-lobby extension was right). Effects to model when we build it: it changes the stairs/escalator appearance, elevators cannot stop on the extra lobby floors, and the floor number does not appear in the shaft there. Lobby is $1,250/segment (1-4 segments, $5,000 for a full 4-segment level), infinite capacity, not deletable, not placeable underground. So the TDT header lobbyHeight byte (1-3) is player-controlled and the round-trip must carry it; our single-story model is a real Classic parity gap (backlog `lobby-height`, now CONFIRMED). Owner could not trigger it in the harness (Ctrl modifier timing); a headless Ctrl-place test to generate a multi-story-lobby fixture is a follow-up after the export work.
- (outcome) Condo-departure RECONCILIATION party (Samus/Cloud/Winston/Boundary/Grumbal, 2026-07-09): read against main-as-shipped, not the stale epics doc. Two of Epic 2's own rulings OVERTURNED because the code they assumed doesn't exist: (1) the 'accumulator with decay' fuse is dropped -- arch-condo-stickiness already shipped the patient fuse as a gentle erosion RATE (CONDO_NOISE_EROSION 0.054) that satisfies the same 'spikes never evict' intent; (2) the '$0 resale sting' was FACTUALLY WRONG -- vacate() charges the buy-back (householdPrice) then CLEARS everOccupied so it resells FRESH; the sting is the repurchase, and Winston's self-scaling-sink point makes it the real flight-risk cost. Grumbal: speccing a punishment the engine refuses to implement. Boundary caught the second tension: condo-eviction (relocation) shipped v1.12.0 on the SAME vacate spine, so Epic 2 as a new mechanic is fully redundant. Verdict: COLLAPSE Epic 2 from build to VERIFY+DOCUMENT (coexistence test: relocation roll skips vacating units, isRelocation guard blocks neglect hijack; both ride one buy-back; document the sink). Also swept the epics doc for main's other overtakes: calendar no longer ratified-out (classic-calendar-parity P1 fast-follow), schedule bytes now emit DEFAULT not zero (v1.14.0).
- (outcome) CLASSIC CALENDAR PARITY implemented (v1.15.0, branch simtower-classic-calendar-parity, spec-first). Party VERDICT (Grumbal/Samus/Cloud/Boundary/Winston/John, session mode): full canon calendar for CLASSIC (12-day year, 3-day quarter = 3-day week, weekend = trailing 1-of-3), display AND cadence. Grumbal opened "you're rebalancing the whole economy to fix a date label" -> surfaced the split-calendar option; Samus+Boundary killed it: the RHYTHM is 1994 (weekend/rent every 3 days is the pulse, and a re-imported real save would collect rent at times the retail game never would, breaking the TDT round-trip); AND canon-AMOUNT parity is blocked (the 1994 rent table is in the un-OCR'd German manual). So the crux is INCOME-INVARIANT RESCALE: per-collection amount x periodDays/REAL_WORLD.period, so money-per-in-game-day is unchanged (canon rent 1/30 every 3 days, maint 1/10 every 3 days). Cloud's seam: ONE mode-resolved Calendar (new src/engine/calendar.ts CANON/REAL_WORLD + resolveCalendar), where Modern real-world = today's constants EXACTLY (factor 1.0, byte-identical, the contained-risk safety net -- the Boundary "keep the existing path byte-identical, add the new beside it" pattern that has won all project). Winston's load-bearing catch: the change is ATOMIC -- onDay() collects rent on clock.quarter change, so flipping the quarter without the rescale = 30x income bug; the mode gate is what makes atomic safe. Modern gets a New-Tower toggle (canon vs realWorld, persisted `modernCalendar`, default realWorld). Killed the incoherent day/30 maintenance "month" (longer than a canon YEAR) -> rides calendar.maintPeriodDays. NO SAVE_VERSION bump (decided during impl): calendar is a pure fn of mode+modernCalendar (both in-save), nothing in save DATA migrates, and existing Classic saves adopting canon is the intended FIX not a regression to grandfather (full suite passes unchanged under canon = proof). EventSystem: only maybeSanta had per-year cadence (year/360) -> made calendar-aware, real-world byte-identical. Files: calendar.ts (new), Clock.ts, Simulation.ts, EconomySystem.ts, EventSystem.ts, types.ts, UI.ts/main.ts/saveLoad.ts (toggle), calendar.test.ts (20). Spec: gdd-/arch-classic-calendar-parity-2026-07-08.md.
- (outcome) gds-code-review (3 layers) on the calendar change: 7 PATCHED, 2 deferred, 2 dismissed, all gates green (951 tests). Best finds: (Blind) the rescale divisors were bare 90/30 literals not LINKED to REAL_WORLD.quarterDays/maintPeriodDays -- the "byte-identical" invariant was protected only by a coincidence of two unlinked constants; now referenced structurally. (Edge Case, CONFIRMED) emitLunchRush (main.ts) still hard-coded a 7-day week (% 7 >= 5) with a comment claiming it matched Clock.isWeekend -- the ONE isWeekend consumer not routed through the calendar; fixed. (Blind+Auditor) Clock.dayName was calendar-blind so the canon weekend slot rendered "Wed" contradicting formatRetroDate's WE; now real-world keeps Mon..Sun, canon labels WD/WE. (Auditor) restored the exact "Monthly maintenance paid" string for real-world (canon drops "Monthly" since it has no month) so the byte-identical path keeps its copy; added the missing maint-invariance + cadence tests; reconciled the arch doc (wire value "canon" not "short"; payMaintenance rounds per-item to keep money==ledger, exact because every maint constant is a multiple of 10). Dismissed: collectRent's Math.round is a no-op for integer rents (real-world byte-identical holds); "Monthly maintenance" had no other consumers (grep). Deferred: 999-year day-counter roll unimplemented (out of scope); the trailing-slots weekend model can't express a NON-trailing phase (contingent on harness). REMAINING before merge (owner-driven): harness weekend-PHASE validation (which canon day-slot the real game treats as weekend for a known currentDay -- pinned to trailing slot with a documented TODO); date round-trip already proven (day 1280 -> Year 107).
- (event) Owner noticed while I touched the lunch-rush gate: does "Lunch rush" actually send office workers to food and back? IT DOES NOT -- today it's cosmetic (bulletin + timePacing clock-crawl + a flat trafficAppeal 1.15 food-income bump). The crowd model (Crowd.ts spawnTrips) is LOBBY-CENTRIC and one-way (every trip lobby->X or X->lobby, person despawns on arrival); at lunch it only spawns lobby->venue and occasional lobby->office. Owner asked to make office workers go to food venues and RETURN to the office. Scoped as a SEPARATE follow-up PR (owner's call, keeps the calendar review valid). Backlog `lunch-office-food-trips` (P2): during clock.isLunch() add trip options staffedOffice->open food venue and food venue->staffedOffice. Nuances: persons are one-way (aggregate flow like the morning/evening commute abstraction, not a tracked individual); food income is appeal-based (already bumped), so this is CROWD REALISM + real noon transport stress, NOT a second income source (don't double-count). Gameplay-feel change -> needs its own gds-review + Samus feel check.
- (outcome) PR #170 (classic-calendar-parity, v1.15.0) MERGED to main as 50c4109 (merge commit, no squash). Ship state: 981 tests, calendar composes cleanly with PR #172's floor changes on top. Two full Copilot review rounds; all 16 threads resolved same-session (3 initial + 13 round-2). Best round-2 catches: (a) Blind Hunter's earlier concern the "byte-identical" claim was protected only by BARE literals 90/30, Copilot posted the mirror concern for the constructor, fixed by clamping Simulation.modernCalendar to "realWorld" for Classic (persistence-edge honesty next to the UI-edge clamp we already had); (b) faqComplete log-filter for maintenance messages was canon-only ("Maintenance paid" startsWith) and would miss real-world's "Monthly maintenance paid", fixed with a case-insensitive regex; (c) `emitLunchRush` inline "weekday, once" comment lied about the calendar-aware gate below. Also swept em-dashes out of every new file I introduced (calendar.ts + gdd/arch docs); saveMigration.ts's em-dashes are pre-existing and grandfathered. Modern-realWorld + Classic-canon end-to-end REGRESSION suites added to calendar.test.ts (+12 tests) at owner ask, covering: structural factor==1 for real-world (rent + maintenance separately), day-90 rent lump landed, day-30 maint beat, canon 1/30 rent and 1/10 maint structural factors, day-3 rent + isolated day-3 maint (elevator-only tower), per-in-game-day rent invariance across calendars, dayName labels [WD1, WD2, WE1] vs [Mon..Sun], persistence, RNG short-circuit. Fixtures use star=1 (fires gated) after a diagnostic test found a fire had gutted the office at star=5 mid-run.
- (outcome) Verticopolis-vs-retail SIDE-BY-SIDE evidence in the PR body: harness loaded MYTOWER.TDT (saved currentDay 60), captured the retail 1994 game's date stamp at two independent post-load points (day 63: "1st WD/2Q/6th Year"; day 67: "2nd WD/3Q/6th Year"), then rendered Verticopolis via the pinned Playwright Docker image at those same currentDays and got byte-identical strings. Two data points pinned both weekday slots + a quarter boundary + the year; the weekend is the trailing day-slot (day % 3 == 2), confirming CANON.weekendDays: 1 with no code change needed. Screenshots committed under _bmad-output/planning-artifacts/reviews/calendar-parity-2026-07-08/ (harness + verticopolis PNGs), referenced by raw.githubusercontent URL in PR body. Followup calendar.ts comment update from "must be confirmed" to past-tense with the harness-validated pin.
- (dynamic) User caught the "Lunch rush" was cosmetic while I was touching its gate. The bulletin + timePacing clock-crawl + a trafficAppeal 1.15 food-income bump fire, but the lobby-centric one-way crowd model (Crowd.ts spawnTrips: every trip lobby->X or X->lobby, person despawns on arrival) never sends office workers to food and back. Scoped as followup lunch-office-food-trips (P2, next). Nuances captured: persons one-way => aggregate flow like existing commute abstraction; food income already appeal-bumped => new work is CROWD REALISM + noon transport stress, NOT another income source; respect 2-ride cap.
- (outcome) TOWER-WIDE MEAL CADENCE shipped (v1.16.0, PR #173). Four windows (breakfast 6-9, lunch 11-14, dinner 17-20, late-night 21-24) drive real transport pressure from every eating population (office workers, condos at 0.3x weight, hotel guests, on-shift staff). Aggregate outbound + lagged return options folded into the existing weighted spawnTrips pool; MAX_PEOPLE cap self-throttles. Cinema stays late-night nightlife (not a meal venue). Staff on-shift single-sourced via new staffOnShift(kind, hour) reading HK_SHIFT_START/END. emitLunchRush -> emitMealRushes covering breakfast/lunch/dinner with tenant-count floor 30 and per-kind lastMealRushDay latch. Dinner clock-crawl added at 18:00-18:30 by splitting the 17-21 timePacing period (day total stays 2600 frames). Economy byte-identical: no dailyTrafficIncome touch, no trafficAppeal change. Value is TRANSPORT STRESS not money -- spatialCongestionByFloor + updateSatisfaction erosion compose for free. Party (Samus/Cloud/Winston/Sally/Boundary/Link/John/Grumbal) ratified aggregate-flow model with Boundary's "the lie is only visible if we claim otherwise; we will not" (persons stay one-way; return is an independent spawn, aggregate outbound ≈ return over the window without per-person tracking).
- (outcome, OWNER-DRIVEN SCOPE PULL) During implementation I proposed a visible-occupancy render dip so offices/condos/hotels "thin out" during meal peaks; owner saw it for what it was ("all of this work is just showing me that we do need people tracking and that food and venues should count towards pop as per canon") and pulled the fake layer. Two follow-ups logged: (a) `per-person-meal-round-trips` (P2 high, next) -- extend Crowd.Person with a return-leg + eat-timer so the SAME individual leaves the office, arrives at the venue, returns. Once real, `u.occupants` legitimately dips at 12:00. Also expands the hotel gate for lunch/dinner (currently silently zero because hotelFloors is isTenanted-or-asleep and hotels are dirty/empty 11-16 today). (b) `population-census-parity` ESCALATED -- venue customers count as pop per canon (fastFood ~35, restaurants/shops TBD). Together they close the ~2x population gap AND make the meal render-dip a consequence of real people leaving, not a fake, AND substantially improve TDT round-trip fidelity (currently we mirror our own undercount). Owner instinct: "big change but a big parity bump" and "will make the tdt import/export so much better". Right call.
- (outcome) gds-code-review 3-layer on the meal-cadence change: 2 decision_needed (owner resolved), 7 patched, 6 deferred, ~5 dismissed. All 6 load-bearing invariants confirmed hold (economy untouched, MAX_PEOPLE cap holds, weekend correctness inherits from staffedOffices, staff on-shift single-sourced, aggregate flow untouched, no per-person tracking added). Best patched finds: (D1) Blind Hunter caught that Math.round(count * 0.3) collapses to 0 whenever count = 1 -- the middle 30% of every window contributed ZERO condo trips even though aggregate integral was correct. Refactored to `rng.chance(pool.weight)` gate per option, smooth shape at every phase, aggregate unchanged. (D2) Edge Case Hunter caught that hotels are declared as lunch/dinner origins in MEAL_MIX but the hotelFloors bin (isTenanted || asleep) is EMPTY 11-16 today (hotel guests only asleep 17-08, checkout at 08 leaves rooms dirty/empty). Owner deferred the gate expansion to the per-person-tracking follow-up; MEAL_MIX declaration stays as the design intent. (Auditor) Two test-plan items were "marked done" with no actual coverage: weekday-lunch positive assertion (AC 4) and bulletin cadence (AC 11). Also the "weekend correctness" test only re-asserted a pre-existing invariant (staffedOffices empty on weekends) rather than counting office-origin trips. Added 5 new tests: weekday-lunch positive, weekend-outcome (counts office-origin trips), housekeeping shift-gate end-to-end (catches removal of the .filter(staffOnShift) line), return-lag ASYMMETRY (first-half outbound-dominant, second-half return-dominant), plus a 12-test `decideMealRush` pure-function file (extracted from main.ts so the bulletin edge cases are actually testable). Also caught: arch §8 had 300/300/400 = 1000 frames for the dinner-crawl split which would have blown the day's 2600-frame budget by 600; the shipped code correctly chose 120/160/120 = 400. Arch doc corrected to match reality. Full suite 1034 tests, all four gates green.
- (event) Stranded-condo party (Samus/Cloud/Link/Sally, player save SixSeven): attemptMoveIns gates on isFloorServed (structural) not floorReachable (2-ride rule), so 56 condos sold for $10.84M on floors 31-44 that are 3 rides out; 168 phantom residents. Inspector already said 'too far, no one travels here' while the game sold anyway (Sally: 'we wrote the honest sentence and contradicted it with money').
- (outcome) CONVERGED fast: (1) gate ALL tenant kinds (office/condo/hotel) on floorReachable in attemptMoveIns, one line after cheap checks; (2) per-pass Map<floor,bool> memo, BFS only for served floors, NO second cache generation (Cloud's CSS-generations callback); (3) widen the daily stranded NUDGE to floors with EMPTY tenant units + reword copy (Sally cashed 'hurt them, but inform prior': the post-fix player with unsellable empty condos got zero feedback), stats-modal row keeps leased-only meaning (two-scope strandedFloors); (4) existing saves untouched, no retroactive eviction/buy-back, no migration (Samus: demand pressure not eviction, parking-penalty doctrine); (5) ruled a PARITY FIX under the ratified two-ride rule, patch bump. Express-to-sky-lobby is the canon remedy the 1994 game was shepherding toward.
- (dynamic) Samus opened vindicated from the condo-churn party ('the house was selling bets on floors players can't walk into'); Sally's leased-vs-empty nudge catch was the round's best find (strandedFloors/isTenantFloorUnit only counts occupied floors, so the exact player the fix creates never gets the advisory); Link happy the all-kinds ruling was also the smallest diff; Samus closed 'spec it before Grumbal hears we converged this fast.'
- (outcome) gds-code-review 3-layer on the stranded-move-in fix: 5 patched, 1 deferred, 5 dismissed. Best find (ALL THREE layers independently): the rentable scope's state===empty let a dirty hotel room on a stranded floor escape the advisory; fixed with isOperational per the spec's own frozen wording. Edge Case Hunter also caught the gate comment falsely claiming office rent honors the two-ride rule (collectRent gates on isFloorServed only; now documented as a deliberate grandfather). Blind Hunter's basement-gate and hotel-population-asymmetry claims both refuted in code (NO_BASEMENT_KINDS; hotels have population>0). Deferred: single tower-wide stranded latch masks a second floor stranding while the first persists (backlog). BONUS from the fix: phase2's endgame fixture was silently broken THREE ways (sky lobbies never placed over floor tiles, express star-gated before star set, shafts clustered past the 79-tile walk) and only won because unreachable floors resold instantly, the exact bug under repair; rebuilt as honest 23-shaft 3-group zoning, wins with zero churn. Owner asked for full regression+shift tests: reachable->stranded relapse, latch re-arm cycle, dirty-room scope, gutted-only silence, info severity. v1.18.2, PR #185, 1105 tests.
- (outcome) COMMERCIAL VENUE OCCUPANCY PARITY shipped (v1.18.3). Party (Samus/Cloud/Link/Indie, session mode): three items shipped, two backlogged. (1) `FACILITIES.fastFood.population` 35->25, `FACILITIES.shop.population` 35->20, restaurant stays 35 -- footprint-proportional ambient occupant counts (footprints: shop 12 tiles, fastFood 16, restaurant 24). Design diverges from 1994 Finance Window (flat 35/unit); live census uses customersIn (unaffected). (2) `updatePresence()` default case now gates `u.occupants` on `isOpenAt(u.kind, this.clock.hour)`: a tenanted-but-closed venue shows 0 occupants so the heatmap and lit-window sprite go dark after closing time. Non-commercial kinds in the default branch are safe (isOpenAt returns true for unknown kinds; cinema/partyHall have population:0). (3) New `venueOccupancy.test.ts` (9 tests) guards population values, open-hours occupants, closed-hours occupants, and hour-boundary transitions. gds-code-review 3 adversarial layers: 3 dismissed (non-commercial-kind gate + intentional-export-change + deliberate-canon-divergence), 1 patched (Clock(0) trap in setHour test helper: `hour * 60 || 1` to avoid the constructor's 0->7am guard), 1 dismissed with comment added (lastHour-not-reset latent hazard, safe because tests use fresh fixtures). Gates green: typecheck/lint/1121 tests/build. Backlogged: `cinema-occupancy-display` (P2 -- cinema pop=0 gates it out of occupancy display even when customersIn>0; needs smarter predicate); `venue-peak-hour-scaling` (P3 parked -- hot path churn + redundant with customersIn; revisit after per-person round-trips).
- (event) PR 184 takeover party (Samus/Cloud/Link/Sally, Grumbal heckling from the doorway): ruled on 7 census questions from the adversarial review of Copilot's commercial-customersIn PR.
- (outcome) Verdicts: shop-never-increments ACCEPTED for PR + shopping-trips backlogged; eater double-count RATIFIED as 1994-additive canon; decrement-at-finish KEPT (single-drain invariant); TDT commercial vacancy DEFERRED pending Wine-harness canon check (does 1994 commercial ever go vacant?); recycling lunch-flap backlogged P3; stranded-commercial milestone/heatmap gates ACCEPTED as intended; version stays patch 1.18.3 (hotel-census precedent).
- (moment) Samus to Grumbal: 'It's CANONICALLY fictional at noon.' Room's new shorthand for parity-over-purity calls.
- (outcome) Retail-subtype visuals design session (PR 184, follow-on): parametric config keyed by canonical subtype name; kind silhouette sacred (band/awning/wall carry the variation + one prop); no RNG in draw (subtypeIndex only); undefined subtype = legacy look byte-identical; u.subtype must join the room bake sig (Cloud caught the Change-variety repaint bug); gallery gets all 21 labeled; Sally holds a legibility veto on the minted screenshots.
- (outcome) Retail variety visuals SHIPPED (v1.19.0, PR 184): 21 distinct looks passed Sally's legibility veto after three hue fixes (Chinese Cafe to lacquer, Pet Store to brown, Post Office to postal blue). Review round caught two real engine bugs beyond art: rerollSubtype never triggered a repaint (now bumps meal-overlay revision) and a venue's first open hour rendered dark (economy now stamps occupants at the empty-to-occupied flip). Codex independently found the venue capacity gap; capped both at spawn and arrival.
- (outcome) Per-unit variety party (independent proposals from Samus/Sally/Cloud, then the room): GEOGRAPHY seed wins (hash(kindSalt + (floor-minFloor)*GRID.width + x)); Cloud's TDT-renumbers-ids evidence flipped Sally off hash(u.id). Bound: office Daylight Grays + wall-item swap + seeded desk plant; condo four Home Plasters + TV-weighted right-slot swap; hotel Linen Sands/Suite Gold + muted bedspread trio (brass evicted from Sally's amber-gold state corridor); cinema audience de-cloned (Grumbal's find); anchors double-weighted; luminance clamp +/-10 per channel; desk-row phase CUT (Samus's frozen rhythm beat Sally's safe-list); floors/lobby untouched. One recorded bend: condo wall consumer moves from hash(u.id) to geo seed, the commissioned widening.
- (outcome) Owner pushback round 2 (mixed color-vs-composition variety): party retired palette-only variants as a concept. New law (Samus's sentence): if the diff is only visible in a color picker, it isn't a variant. Offices and condos get three true layouts each plus mirroring (Cloud's free-geometry find); hotels ruled DELIBERATELY uniform (Sally: the room's job is broadcasting state), bed-mirror only. Samus owned underselling 1994's office furniture shuffle.
- (outcome) Owner caught the stretched-mattress bug (doubles/suites = one long bed). Party: grades ARE the geometry (single one bed, double TWO beds with a real gap, suite wide bed + sitting area); Sally clarifies her uniformity ruling was same-grade-uniform, never same-across-grades. Cloud's contract made testable: everything visual derives from persisted fields; save-reload paint-identity suite (paint, serialize, deserialize, paint, byte-compare) across all kinds and varieties.
- (outcome) Layouts edge-hunter round: accounting/mirror discipline CONFIRMED; patched the flipped tray/lamp state cues (moved outside the mirror), added the missing kind salt to geoVariant, third exec side desk, dining-layout diner count honors household size. paintPersistence.test.ts is the new executable contract: every kind and variety paints byte-identical after .vctower reload and TDT round-trip.
- (decision by party) 2026-07-12 chrome-restructure party (Samus, Sally, Cloud, Link): Game panel Export/Import row removed (Saves modal footer already hosts both); freed row becomes a full-width settings button (btn-settings, gear glyph, label Settings; Samus lobbied for 1994 'Options' and lost to clarity). Settings modal = Sound sliders + Reduced motion + Steady clock; Help keeps how-to copy, keyboard, report link, About, Replay Getting Started. Mute stays top-bar (panic button). colorblindCue exposure deferred to backlog. Footer guard test re-pins to [replay-onboard, close]; slider tests relocate; 02-help.png regenerates + new settings screenshot scene.
- (decision by party) 2026-07-12 screenshots follow-up (Samus, Sally, Cloud, Link): docs set regenerates wholesale on marker push so panel/drawer chrome updates free; e2e visual baselines untouched by the chrome change (verified scope). Real gap Link caught: the Saves dialog, now the only Export/Import home, had zero screenshot coverage; added 02c-saves scene with a populated slot (Sally's condition). 02b-settings guard hardened to a settings-only selector (#modal #vol-music). No mobile settings shot (same DOM both form factors), no README change.
- (outcome) Save-parity party (vctower vs TDT view state): ruled camera tile+floor+zoom ride SerializedGame as optional inert 'view' field (seam not framework; absent = center() fallback, no migration). Restore clamps at setCamera/clampZoom seam (Grumbal's hostile-JSON zoom=1e308 carve-out; Sally's per-device viewport clamp). TDT import reads header 0x26/0x28 instead of skipping; TDT export writes real view, default when absent; zoom doesn't travel to 1994 (format holds two words, two words travel - Cloud). Speed/selection/panels vetoed by John+Link scope fence.
- (outcome) Log-cap party (PR195 defer): ruled LOG_SAVE_CAP rises 100->300 to equal the ring cap. Yui: three numbers pretending to be two; Cloud: a snapshot holding less than live state is a superseded rule still standing; Dana killed the undo-only serialize path; Grumbal's tripled adversarial ceiling (124KB) dismissed as covered by the existing 300x400 restore caps + 64MB bomb guard ('covered by yesterday's carve-out'). ~3KB per save.
- (outcome) Saves-modal party (owner: anything else in the save + show mode?): ruled the crate is FULL, no new save fields (prefs/speed/undo re-vetoed, crowd occupancy stays engine backlog). The modal under-displays: add Classic/Modern chip (both labeled, Sally: 'unlabeled jars') + in-game day to slot rows; John vetoed appVersion and calendar in the modal. Samus's render-from-data tower thumbnail -> backlog as Sally's IOU. Grumbal demanded the forged-mode pin (isGameMode coercion).
- (event) Party convened on the bot-review NIT CHURN blocking two ready-to-land TDT PRs (#204 lobby/floor export v1.25.1, #200 No-Rate + lastQuarterMoney v1.26.0). Both game-verified via the Wine harness, but Copilot/Codex file a fresh low-severity nit every force-push (~5 rounds). Room: John (PM/shipping), Cloud (architect), Boundary (seam invariants), Grumbal (adversary), Paige (tech-writer). [session mode; resolver blocked by py3.11.]
- (outcome NIT A - FIX) `classFromRent` JSDoc said it maps to "the four 1994 lease classes" while the code returns a fifth (class 4 = No Rate). Ruled a CORRECTNESS nit about the doc (the comment lies about the code), not a style whim - and WE introduced it by adding the class-4 branch. Paige drew the line vs #178: that was a stale comment nobody touched; this is our own debt. Fix: reword to "four rent levels plus No Rate".
- (outcome NIT B - FIX, Boundary's law) A forged save can set `noRate: true` on an UNPRICED kind (shop/fastFood) because Classic `deserialize` preserves any literal true. Grumbal's reflex ("inert + forged-only -> reject") was overruled by Boundary: `noRate` is a priced-unit INVARIANT - import gates on `rentConfig(kind)`, export reads it only off priced kinds, the move-in guard only fires for priced kinds; deserialize is the ONE hole, and it's exactly where untrusted input enters. Fix mirrors import: `rentConfig(u.kind) ? coerceNoRate(u.noRate) : undefined` - one predicate, already imported, makes the third copy of the rule agree with the other two. Grumbal conceded on record: worth doing because it completes an invariant across the trust boundary, NOT because a bot frowned (no NoRatePolicy class = not gold-plating).
- (outcome STANDING RULE - ratified) To stop the churn: (1) the WORTH-A-CHANGE test: FIX a nit only if it is a correctness/accuracy/invariant/security gap (a doc that contradicts code counts; an invariant hole at the trust boundary counts, even forged-only); REPLY-AND-RESOLVE (won't-fix) a nit that is phrasing preference, a re-raise of a documented intentional decision, or a whole-context blind spot. (2) BATCH: accumulate a PR's fixes and push ONCE - every force-push re-arms the reviewer (Grumbal's whack-a-mole: each swing spawns a mole). (3) JOHN'S FLOOR: after ONE clean batch (fixes pushed, threads resolved, CI green) the PR is landable on the OWNER'S CALL; a nit arriving after that is a follow-up issue, not a merge blocker - we do not owe the bot infinite quiet rounds. (4) Boundary's carve-out: a nit tagged security or data-corruption jumps the queue regardless of round count. (5) Grumbal's rider: version-number drift from a rebase is a MECHANICAL reconcile (doc + title + body must all say the same number - Paige's "someone fixes two of the three" trap), never treated as a review discovery.
- (dynamic) Boundary's seam bit struck a fourth time this saga ("no unpriced unit ever carries noRate is either true everywhere or a lie I have to remember" joins version:1-is-a-lie and sim-is-a-guest), reframing a "forged-only inert" nit into an invariant worth closing. Grumbal lost the war (both nits fixed) but banked the loop-breaking rule and the version-drift-is-mechanical rider. John got his shipping floor. Running bit continues: Copilot chasing our own drift to ever-finer boundaries, now formally capped.
- (event) Party reviewed the pre-approval spec `spec-express-elevator-parity.md` (two Express Elevator parity fixes vs 1994: width 4->6, lobby-locked stops). Room: Grumbal (adversary), Cloud (architect), Boundary (seam/forged-input), Samus (game-feel), John (PM/shipping). [session mode.]
- (outcome item3 - RATIFIED w/ hedge) 6-tile width is a CALIBRATED measurement (8.00 px/tile from two shafts placed exactly 40 tiles apart; express = 48px = exactly 6.00), not a guess. Grumbal won a cheap hedge: acceptance gains a SECOND harness confirmation before merge (place a room flush against the express, read the gap). Ship the 6, witness it first.
- (outcome item1+6 - taken together) BOXED-IN express keeps its legacy 4-wide footprint (the heal; never haul a landmark shaft across the lot on load - Cloud; owner sign-off gate on any far relocation stays). Samus flagged a lone 4-wide express among 6-wides "reads as a bug" -> Boundary: the sin isn't keep-at-4, it's SILENCE. BUT Grumbal killed the express-only bulletin: the standard 3->4 widen has shipped SILENT since day one, so an express-ONLY note is favoritism/inconsistency (worse than consistent silence). JOHN'S RULING: bulletin CUT from this spec; a general "N shafts widened to their 1994 footprint" note (covering standard AND express) is its own change, folded into the backlog's e1c-migration transparency row. This spec does width + stops only.
- (outcome item5 - Boundary's seam law, ADD TASK) Engine `setStop` refusal is the right seam for the express lobby-stop lock (invariant holds against every caller; UI just stops offering it). BUT the IMPORT door bypasses setStop (tdtImport writes skipFloors directly) - a forged/foreign TDT could encode an express stop on a non-lobby floor. Same class as noRate-on-a-shop. NEW TASK: import/deserialize must COERCE an express's stops to lobby-only (endpoints exempt). Grumbal concurred (the real game never writes a non-lobby express stop, so such a save is forged by definition).
- (outcome item2 - RATIFIED) No SAVE_VERSION bump: the v5 `widenLegacyElevatorShafts` re-heal is idempotent (`fp.w >= canonW` skips a done shaft). Cross-build wrinkle to DOCUMENT (so nobody "fixes" the clamp later): a new-build save (express width 6) loaded on an OLD build clamps back to 4 via the "widths only ever grew, above-catalog is forged" rule, then re-widens when reopened on a new build. Graceful degrade, not corruption. Width is the same field, not a format change - bumping would needlessly skip the v1->v2 reflow.
- (outcome item4 - RATIFIED, non-issue) Wider express = zero balance change (capacity 42, car count, throughput all width-independent; cost is per-kind not per-tile). Purely spatial; existing towers eat 2 tiles on load, reconciled by the migration. Express is already a 3-star/400k late-game commitment. Samus: it finally reads like the chunky freight monster it should be.
- (dynamic) Boundary's "the invariant is true everywhere except the door untrusted data walks through" struck a FIFTH time this saga (joins noRate-on-a-shop, version:1-is-a-lie, sim-is-a-guest, no-unpriced-noRate) - the import-coercion catch was the room's highest-value find. Grumbal lost the measurement fight (calibration held) and the bulletin fight went his way (consistency over favoritism); John's shipping floor cut one rider and added one real task. Net: tighter spec, one genuine forged-input hole closed before it shipped.
- (event) Party ruled on shipping two ready elevator changes: PR #209 (storage payload-size fix, v1.26.3, ready-to-merge open PR) vs the local express-elevator-parity branch (engine+ui+render, v1.27.0, needs visual-baseline regen). Room: John (PM), Cloud (architect), Grumbal (adversary), Boundary (seam).
- (outcome RATIFIED - TWO separate PRs, #209 first) Unanimous: separate, not combined. Different LAYERS (storage byte-fix vs engine/ui/render+migration), and combining would make one review hold "payload byte size" and "glass shaft alpha" at once = worse review (Cloud). DECIDING FACTOR (Grumbal, who normally argues AGAINST more PRs to avoid bot-nit churn): the express PR drags a `docs/screenshots/**` visual-baseline regeneration (`[update-baselines]`) behind it; welding that to #209 makes a clean done storage fix HOSTAGE to a screenshot diff. Never chain a fast thing to a slow thing that shares no code. ORDER: #209 lands first (ready now, no baselines), express pushed+PR'd immediately for review but lands AFTER #209 + AFTER its baseline regen. Both on the OWNER's call, neither auto-merges (standing rule). Boundary's rebase note: both touch package.json (1.26.3 vs 1.27.0) -> a ONE-LINE mechanical reconcile to 1.27.0 on rebase, never logged as a review finding (the ratified version-drift rule). Grumbal's rider: fire the baseline marker ONCE and wait, don't nudge it repeatedly.
- (event) Mobile zoom-out party (owner: 'I should be able to zoom out more than this on mobile', 82-floor/3849-pop save attached). Room: Samus (game-feel), Cloud (architect), Link (dev), Sally (UX/legibility veto). Constraint: VIEW_ZOOM_MIN=0.3 caps mobile view at ~43 floors (measured from screenshot: floors 40-82). FLOOR=44 world px; GRID -9..100 = up to 113 floors w/ margin. [session mode]
- (decision by party) Mobile zoom-out RULED (owner picked 'fit tower + sky headroom' over fixed-floor freedom / tight-fit). TWO knobs, not one (Cloud's split): (1) TRUST-BOUNDARY hard floor = VIEW_ZOOM_MIN lowered in engine/types (was 0.3) to a low value that fits the biggest legal tower on the smallest phone; still the clamp coerce.ts + save-restore use. (2) GESTURE floor = computed live in the renderer from actual built tower span, so pinch/wheel/keyboard zoom-out stops when the whole tower + a little sky fits, never drifting into void. Pattern mirrors clampCameraY (compute bound from world height, don't hardcode). Grumbal's two hazards folded in: span measured built-top to built-bottom INCLUDING basements (not just above-ground), and a MIN span floor so an empty/lobby-only tower can't divide-by-zero and fling the camera. Helper lives in the pure cameraBounds family, unit-tested without a canvas. Gesture paths (pinch/wheel/keyboard) clamp to dynamic min; setCamera/save-restore keeps the static trust-boundary clampZoom. Grumbal's 3-copies-of-the-rule reminder: viewStateParity.test.ts pins VIEW_ZOOM_MIN in 2 places + coerce.ts clamp.
- (outcome) Mobile zoom-out SHIPPED (v1.29.0). fitZoom() tower-aware gesture floor + VIEW_ZOOM_MIN 0.3->0.06 hard floor; wired into TowerEngine zoomAt/zoomBy. gds-code-review (Blind Hunter + Edge Case Hunter, no Acceptance Auditor/no-spec): 3 patched, 1 deferred. PATCH-A (both layers, the room's top find): clampGestureZoom could FORCE a zoom-IN on the next gesture when the camera sat below the tower-aware floor (rotate landscape->portrait grows viewHeight and raises the floor; or a cross-device restore). Fixed WITHOUT touching applyView (which must stay an exact viewState inverse - parity tests pin zoom 1.7/0.3 verbatim) by dropping the effective floor to the current zoom when already below lo: floor now only stops further zoom-OUT, never yanks inward; normal floor re-engages once back above it. PATCH-B: manifest is orientation:any, so a maxed 110-floor tower must fit a SHORT landscape viewport too; 0.08 clipped ~14 floors at ~360px, lowered to 0.06 (fits 116 floors down to ~306px). PATCH-C: the framedFloors asserts algebraically cancel FLOOR (tautology catching only 'no clamp fired'), added a by-hand concrete-zoom pin (567/((82+6)*44)=0.14644) + landscape max-tower assertion + 4 clampGestureZoom behavior tests (no-forced-zoom-in, re-engage, basements-count). DEFER-D->backlog: dynamicMinZoom does 2 uncached O(units) scans per pinch-move frame (~26k iters on a 13k-unit save, ~0.1ms, no defect); fix later with a revision-cached built-floor span on Tower. Blind #2 (first-gesture forced zoom-in) REFUTED: default zoom 0.9 > MAX_FIT_ZOOM 0.6, fresh start never snaps. Gates green: typecheck/lint/1412 tests/build.
- (event) Render-perf party (Cloud, Link, Samus; Grumbal walk-on) on the owner's Pixel 8a rush stutter (sixseven_8 save: 4-star, 5,085 pop, 20,155 units). Diagnosis on the table: sim exonerated (tick ~7.5ms/min flat, 55-112ms total per 30s window), frames ~1s at min zoom from ~2,500 actors, and 1,635 room actors each owning a PRIVATE canvas texture vs 8-16 GPU texture slots per batch (draw-call storm; the unfinished half of Cloud's TileMap move).
- (outcome) Three-story fix ratified, in order: S1 zoom-gated cull of crowd/walkers/cars/trucks (hysteresis, threshold = figure sub-legible ~3 screen px, skip their per-frame position loops while culled); S2 shared IMMUTABLE room bakes keyed by the existing draw signature (refcounted cache, evict at zero, animated rooms keep private canvases, look-change = reference swap never repaint); S3 defer the hour-boundary render reconcile one frame (halves the on-the-hour hitch). Engine onHour amortization REJECTED as a rider: load-bearing for determinism/golden master; backlogged pending a checkpoint-inputs design consult.
- (dynamic) Grumbal's shared-canvas mutation trap ("flagDirty repaints every office in the tower") forced Cloud to make immutability the S2 contract; Samus extracted two strings: named cull-threshold constant with the sub-legible rationale, and zero engine-number changes ("a camera-and-paint patch"). Link's acceptance line for S2: visual baselines byte-identical + texture count collapses ~1,600 to dozens, proven with the rush probe on the owner's save per story.
- (event) Render-perf party reconvened (2026-07-15): the pixel-hash census (738 day / 1,302 night unique room bitmaps on the owner save) falsified CAP-2's shared-bake premise; Devon, Ash, and Rio ruled unanimously for region composition. Ash's 17:00 upload arithmetic beat Devon's ship-without-a-budget instinct (bounded drain is a constraint, not tuning); Devon's ordering argument won CAP-3 its place ahead of the region story; Rio extracted the full-tower scene baseline precondition and owner-on-device sunset acceptance.
- (dynamic) Rio's framing stuck: the census did not discredit the variety law, it priced it; regions pay in engineering instead of soul. Ash keeps promoting per-thing laws to per-layer laws (RoomRec's repaint-in-place becomes region identity I1).
- (event) onHour consult FINAL roundtable (Cloud, Link, Samus, Indie, John; Grumbal walk-on) on the ratified spec-onhour-boundary-cost; owner delegated: whatever the full party agrees to. [session mode]
- (outcome) Index shape REFINED: Link's lazy revision-keyed memo (Map<unitId,boolean> + rev stamp, strict equality, init -1) computing through the EXISTING nearestKindWithin beat Cloud's eager per-floor index. Rationale: the eager index is the only version that can be wrong (it reimplements cinema-upper-story/lobby-order/band-edge semantics); lazy reuses the truth to fill the cache, makes the watched hour free and the edited hour no worse than today. No new module: the memo lives beside noiseAfflicted in satisfaction.ts; the spec's noiseIndex.ts line is amended in its memlog, not silently ignored. Differential test SHIPS anyway as cheap insurance pinning memo path === fresh path across every mutation.
- (outcome) Cache home RULED: Simulation transient (elevatorUtil precedent), NOT Tower fields; noise is a sim rule and Tower's five rev-caches are routing derivations. deserialize builds a fresh Simulation so stale-undo is structurally impossible. Cloud's module-singleton ban stands.
- (outcome) Process RULED (John's floor + owner delegation): amend spec memlog + re-derive SPEC.md inside PR #390, mark ready, merge; implementation is ONE separate PR right behind it (/gds-code-review, four gates, patch bump, env-gated bench with a scripted ~20k-unit fixture, never the owner save).
- (dynamic) Grumbal arrived to object and the room deleted his job: the lazy-memo ruling removed the reimplementation his findings would have fed on. His concession line ('I hate it. Do it.') joins the running bits. Cloud conceded shape but extracted the no-silent-spec-drift condition and kept the singleton ban; Samus banked GDD 4.1 keeping a single textual home.
- (outcome) onHour noise-memo IMPLEMENTED and /gds-code-review passed clean: Blind Hunter and Edge Case Hunter found ZERO patch bugs (memo staleness, id-reuse, held-servedSet-mutation, vacateCause pass-through, serialization/undo/determinism all traced clean); Acceptance Auditor SATISFIED on every spec constraint except the dropped lobby-shield differential case (low). Triage FIXED both defer-grade notes rather than backlog them: added the lobby-shield case via a crafted legacy mixed-floor save (deserialize path, since sky-lobby canon blocks it from placement) and a vacateCause 3-arg fallback pin. Golden master unmoved; bench (synthetic 18k fixture, headless) median boundary 38->30ms, 8:00 81->46ms, midnight unchanged (untouched onDay bundle). One PR, v1.34.4 (the initial 1.34.3 collided with main and was bumped; see below).
- (dynamic) The lazy-memo shape paid off exactly as the roundtable bet: because assertParity pins memo===fresh regardless of which scan branch fires, the reviewers had no reimplementation to attack and the whole review collapsed to two coverage nits, both cheaply closed. Cloud's no-silent-spec-drift condition held (the eager noiseIndex.ts line was superseded on the record, not ignored).
- (outcome) onHour PR #403 second Copilot round (all fixture-assertion nits per AGENTS.md 'fixtures must assert their own construction'): FIXED all, and the assertions earned their keep - adding ok-checks to the bench's bigTower exposed TWO silent under-builds the old fixture hid (width-16 condos/fastFoods overlapping at stride 12, and basements laid from B2 skipping B1 so they never connected). Rebuilt the bench as a dense asserted office grid; fair same-fixture A/B on 19,531 units: median boundary 41.2->34.3ms, midnight ~99->96 unchanged (onDay bundle untouched). Also: rebased onto main (e9dd243, #391/#402 merged), version collided at 1.34.3 so bumped to 1.34.4; owner caught both. History reshaped to 2 clean commits per the new keep-history-clean rule.
- (outcome) onHour PR #403 Copilot rounds 3-4: all valid, all fixed. Extracted the duplicated lay/layTile/mustBuild/placeUnit into src/tests/fixtures/towerFixtures.ts (shared by the differential test and the bench, so they cannot drift); replaced the mutator-sweep's unitId/transportId '?? 0' fallback with checked placeId/buildId that throw on the real breach; reworded the noiseAfflicted memo doc to state the true invariant (memo clears strictly on tower.revision, noiseAfflictedFresh is pure of layout) instead of the overclaim 'no query happens between a mutation and the next boundary'; renamed hourCost.bench.test.ts to .bench.integration.test.ts so it lands in the integration tier, not unit (vite projects split on the .integration suffix). Copilot has been thorough and correct on this test surface every round.
- (outcome) Design party (Sally UX, Samus game-design, Cloud architect) on the 'Found a New Tower' rule-set dialog. Complaint: the calendar sub-picker always rendered even though it is Modern-only, and both cards dumped a wall of Classic-vs-Modern feature text. Ratified: two SHORT identity cards (Classic = 1994 pixel-for-pixel; Modern = 'everything in Classic plus what the original couldn't do'); a Modern-only reveal (.nt-modern-only inside a .nt-choice wrapper) that unfolds ONLY when Modern is checked via pure-CSS '.nt-choice:has(input[value=modern]:checked) .nt-modern-only{display:flex}', containing a TIGHT 3-line 'Modern adds' summary + a signpost to Help (full comparison already at help.ts:43) + the calendar pace picker. Classic collapses the region so its radios leave tab order, deleting the layout Cloud's old all-caps 'never make calendar conditional' comment defended. Guard rail: keep realWorld radio checked while hidden (controller reads nt-cal only in Modern). No controller/JS change; integration tests stay green (they set nt-cal .checked directly). User chose 'tight summary only'.
- (outcome) Follow-up party (Sally, Samus, Cloud) on the New Tower declutter's two knock-ons. (1) HELP LENGTH: the Classic-vs-Modern comparison the dialog now points to was one ~200-word run-on <li> buried as how-to bullet 12 of 12. Decision: pull it into its own scannable 'Classic vs Modern' <h3> subsection in help.ts (compact side-by-side: Classic identity vs 'Modern adds' + calendar line), removing the giant rule-set bullet. Reshapes existing copy, no net length gain, so the dialog pointer lands somewhere readable. (2) VERSION IN SETTINGS: room split Sally (mobile muscle memory = Settings>About, and this ships as TWA/Capacitor) vs Cloud/Samus (version is a build-identity fact, belongs in About/splash, Settings is toggles). Converged on 'don't move it, ECHO it': keep splash + Help About unchanged, add ONE muted read-only version line at the foot of settings.ts sourced from the same __APP_VERSION__ (Cloud's veto: must not read from anywhere but __APP_VERSION__ and must not look editable). Samus: 'defending against a move, a quiet echo where players look is a signpost not clutter.'
- (outcome) Party round on 'version in the New Tower dialog too?': UNANIMOUS NO. Reasons: (1) the New Tower dialog is a COMMITMENT surface (which rules?), not an ABOUT surface, and this whole session was about pulling clutter OUT of it ('doorway not encyclopedia'); (2) redundancy knife (Cloud): the splash already stamps the version on the screen IMMEDIATELY before the New Tower dialog on a fresh boot; (3) Sally declined to fight for it using her OWN muscle-memory principle: nobody checks a build number in a 'found a new game' flow, so it's litter not discoverability. Version's three good homes stand: splash (box art), Help About (colophon), Settings (echo where players hunt). SCREENSHOT LAW re-affirmed by Cloud: a version string may only exist behind class .splash-version or .app-version, because pgMaskVersion (scripts/screenshot-page-ops.ts) rewrites exactly those to 'vX.Y.Z' before every capture. So the Settings echo reuses .app-version and is auto-masked; no screenshot-tooling change needed. User also flagged hiding version in all screenshot scenarios, satisfied by that class reuse.
- (outcome) Help dialog completeness + IA overhaul (user: 'you're missing newer Modern things, maybe collapsible?' then 'maybe more things behind dropdowns so Help doesn't start so long?'). TWO changes: (1) COMPLETENESS: audited src/engine/gameRules.ts (the single source of truth, GameRules interface + CLASSIC_RULES/MODERN_RULES) and rewrote 'Classic vs Modern' to cover the FULL divergence set (variant households/size-price/churn, self-relocation turnover, deeper economy = held-space overhead+condo tax+noise erosion+unmet-demand notice, continuous pricing vs 1994 four-rung+No Rate, freer building = escalators-on-office + express-transfer-anywhere-vs-sky-lobby-only, cockroach paid-exterminator vs bulldoze-only, build-refusal-reason-on-hover, livelier crowd = school/sales/weekday-weekend/softer-rain, calendar pace) + a Classic-fidelity report call-out ('pixel-faithful to 1994; if it doesn't match, report below'). (2) IA (party: Sally, Paige tech-writer walk-on, Samus, Cloud): Help was a mile-long scroll answering 5 different questions at once. Ratified Cloud's ONE rule 'essentials open, reference-or-optional collapsed'. Body is now a stack of <details class=help-modes>: 'The basics' (5 essentials) OPEN; 'Going further' (trash/win/parking/cinema/bulk-price/clock), 'Classic vs Modern', 'Keyboard play', 'About' all COLLAPSED. Lead + mouse-controls line and the Found-a-bug CTA stay visible. Modal dropped from a long scroll to ~783px on first paint. Paige's caution honored: Basics still names the win exists (points to Going further) so no summary lies about its contents. About collapsed is safe: version echoes into Settings now and pgMaskVersion rewrites the .app-version span open or shut. Summary styled like the old <h3> with a motion-free ▸/▾ caret. Tests: sectionBySummary() helper; asserts basics-open + 4-collapsed and a phrase-per-gameRules-divergence completeness guard so Help can't silently drift.
- (outcome) Deep review (bmad-code-review: Blind Hunter diff-only + Edge Case Hunter repo-access; no spec so Acceptance Auditor skipped) on the New Tower / Help / Settings change. Verified CLEAN: em-dash/British/AI-marketing copy, both factual cross-refs (New Tower->Help 'Classic vs Modern' section exists; Basics->'Going further' Win bullet), controller read path (querySelector reads display:none nt-cal fine), masking (.app-version caught by pgMaskVersion open or collapsed), integration tests, sole settingsTemplate caller, version boundary, e2e. PATCHED findings: (1) inverted the New Tower :has() reveal to default-visible + hide-under-Classic via :has(classic:checked) so a browser WITHOUT :has() degrades to the old always-visible calendar instead of trapping a Modern player with no calendar choice (same pixels in the pinned Chromium); (2) removed the over-scoping '.help-modes ul{margin-top}' rule that bled onto .help-keys; (3) restored heading semantics lost when Keyboard play/About went h3->summary, by wrapping every Help summary in <span role=heading aria-level=3>; (4) nits: reworded two 'X, not Y' comments, the stale 'between the collapsibles and About' comment, and the '.nt-adds summary' mislabel (it's a label, not a real <summary>). KEPT as intentional: Settings version as a hairline footer AFTER modal-actions (reads as a footer). NOT deferred to backlog (rejected as not-worth-it): caret ::before SR-announcement nit (native <summary> already conveys expand state; broadly benign). BLOCKER: 4 gallery screenshots (02-help, 02b-settings, 00b-onboarding-classic/modern) drift; regenerating in the pinned mcr.microsoft.com/playwright:v1.61.1-jammy container (Docker daemon started in-session) per CLAUDE.md, not host Chromium, so output is CI-equivalent and committable -> lets the PR open non-draft.
- (outcome) Shipped as PR #460 (non-draft, user authorized skipping draft since screenshots were regenerated locally). Regenerated the 4 drifted gallery shots (00b-onboarding-classic/modern, 02-help, 02b-settings) in the pinned mcr.microsoft.com/playwright:v1.61.1-jammy container (Docker daemon started in-session; first attempt failed because npm ci can't reach the registry through the proxy INSIDE the container, so the working recipe is: restore host node_modules, then run the container build+screenshots WITHOUT npm ci, reusing the mounted host modules with PW_CHROME set to the image's pinned Chromium). Exactly 4 images changed, rest byte-identical = determinism confirmed. Version 1.53.0 -> 1.54.0. Subscribed to PR activity.
- (event) Starter-lobby party (Samus, Cloud, Sally, Grumbal, John): owner asked left-edge start for new towers now that the left lot edge gets a plaza (sidewalk/roundabout/fountain replacing the neighbor-building mockup).
- (decision) RATIFIED (owner sign-off): left-edge start REJECTED unanimously (kills tower-shape variety, fights bidirectional camera/build promise, couples scenery to sim geometry - Grumbal's wall between worlds). LIVE decision: mode-split starter lobby - Classic goes canon-zero (1994 starts with ZERO tiles; our 40-tile centered seed was silent training wheels), Modern keeps the centered seed as a documented nicety. Seam: rules.starterLobby resolved at founding. Conditions: fixture decoupling (golden master BUILD_SCRIPT depends on the seed) lands FIRST as its own PR; Classic zero-start ships WITH an onboarding lobby-placement card in the same PR (Grumbal's condition, owner confirmed: 'lobby placement needs to be added to the first time startup on classic'). Owner routing: spec via GDS (gds-spec lane, game-design artifact), and it is a FOLLOW-UP PR after the plaza ships. Plaza polish first: fountain more vibrant + bigger, add lights (lamps), then plaza PR.
- (event) Crane-scale party (Sally, Samus, Link + walk-ons: Mo the player with a 92-floor phone tower, Ansel the 1994 purist with the laminated Kiwizoid FAQ): the new skyline/plaza gave the scene a scale ruler and the 1.7-floor crane failed it.
- (outcome) RATIFIED unanimously: crane scales up FIXED-size (Samus's grow-with-the-tower gimmick killed by Ansel+Sally: the skyline only calibrates if everything obeys the ruler); purist conditions held (no recolor, no new animation, topped-out removal stays). Owner picked k=3.0 after the review found the engine is pixelArt so fractional 2.5 bakes AA softness; integer 3.0 = crisp 5.2 floors. Shipped v1.77.0 (PR #567, merged).
- (dynamic) Ansel and Sally allied against Samus's scaling gimmick ('a sentence I want noted' - Sally agreeing with the purist). Ansel's line: the crane coming down at 100 is the closest thing the flourish has to 1994 restraint. Samus banked the consolation: at 3x the trolley/hook animation is finally watchable theater.
- (outcome) gds-code-review earned its keep on the crane: Edge Case Hunter caught the camera's 2-floor sky headroom decapitating the scaled crane at floors 98-99 (fixed: SKY_HEADROOM_FLOORS derived from CRANE_H); one defer (6.25x decorative raster upload) triaged to #568 gated on #538 telemetry. Owner-save validation: sixseven_16 (26.6k units) imported live, floor-100 bulldoze brought the crane back on 99.
- (outcome) Analytics D-1 REANALYSIS (installed room: Amelia, Mary, Cloud, Boundary, Grumbal, Sally, John), on CURRENT main after an earlier party ran on a ~1100-commit-stale checkout. The full-repository-audit 2026-07-21 already opened D-1 (issue #542): stay on Vercel WA vs migrate to cookieless PostHog, with the AUDITOR recommending migrate-now. Reanalysis with the auditor (Amelia) in the room SUPERSEDES the first party flat cut and both the auditor flat migrate-now. Key correction: the migration case is NOT retention (both options cookieless); its one load-bearing plank is that Vercel is aggregate-only so analytics-report.mjs reconstructs APPROXIMATE percentiles from value-count histograms (real tooling debt PostHog would delete). Mary's kill: you would migrate for EXACT percentiles over ~12 sessions/month, where a p90 is a rumor; the hack is ugly code answering a question the data cannot support at this scale anyway; cheap-to-do (empty tables) is not worth-doing. Boundary: a PostHog project key in a PUBLIC MIT bundle is a forgeable write endpoint, orthogonal to cookieless; Vercel ingestion is first-party keyless. Grumbal: a ~50KB SDK on the boot path collides with the SAME audit number-one risk (render perf, AUD-014). Boundary also re-filed AUD-036: the platform dimension needs ONE boot-event prop (platform: web/twa/ios), not PostHog, so it is neutral not pro-migrate. John: the trigger is SCALE not today (no monetization decision is makeable at 12 sessions). LANDING (Amelia signed): D-1 is not Vercel-vs-PostHog, it is Vercel-NOW, PostHog-on-a-defined-trigger. (1) stay on Vercel now, keep the #533 percentile machinery flagged throwaway-ON-TRIGGER; (2) build the CAP-1 one-file analytics seam now so the future swap is a one-file diff; (3) take the cheap Vercel wins the migration was bundling (platform prop resolving AUD-036, on-device returning/tenure buckets, first-tower funnel, #538 band-bucketing); (4) pre-commit the PostHog migration to an explicit trigger (store-launch / E4 day-one baseline, or a traffic threshold where percentiles become meaningful), recorded as a scheduled swap. Reconciles auditor (gets PostHog when it pays, seam pre-built) and first party (no vendor tax on 12 sessions). Owner still rules D-1 on #542; this is the recommendation, not the ruling.
- (dynamic) Reanalysis dynamics: Amelia (auditor voice) defended migrate-now hard on the percentile-reconstruction debt, then conceded the SHAPE once John forced the what-decision-changes question and Mary priced exact-percentiles-at-12-sessions as worthless; she traded migrate-now for migrate-on-a-trigger with the seam built now. Cloud reframed Amelia readiness as the seam, not a migration. Grumbal objected to how agreeable it got but agreed. The first party flat cut is retired: nobody was wrong about the TOOL, the audit was wrong about TIMING and the first room wrong to call it a flat cut. Note for the record: PR #561 (the first party spec, cut-PostHog framing) was converted to draft pending this reanalysis; its SPEC.md / ADR framing needs re-derivation toward the conditional Vercel-now-PostHog-on-trigger decision before it can represent D-1.
- (outcome) Analytics D-1 RULED (owner, 2026-07-21). Before ruling, the owner asked for fresh data and pipeline validation: a 2-day analytics-report run was dispatched and VALIDATED live (prod serves the insights and speed-insights scripts HTTP 200, the capture endpoint 400-validates a junk body, and every custom event is wired to a real game action via bootstrap/saveLoad/buildActions/main/frameLoop/appBoot/updateFlow). It returned 329 sessions, 197 boots, ~113-plus unique visitors in TWO days, not the audit 12-per-30-days, overturning the reanalysis timing premise (percentiles now have real samples and Vercel is approximating them live). RULING: MIGRATE to cookieless PostHog, seam-first, via a same-origin serverless relay so posthog-js is NOT shipped (near-zero bundle) and no project key is in the public bundle (key server-side); cookieless with an in-memory per-session distinct_id for within-session correlation and no cross-session identity; a measured mobile bundle budget as a hard gate (render perf is audit risk one); a dual-write window before retiring Vercel; and server-side VERCEL_ENV preview-vs-production separation. Retention stays parked (cookieless has no identity). Specced via bmad-spec at spec-analytics-posthog-migration (SPEC plus reverse-proxy.md, edge-fn-setup.md, transparency-note.md); ADR is mobile-PRD decision-log entry 14 (refines entry 13). Cost free at this traffic (PostHog ~1M events/month free, Vercel edge function within the Hobby allowance). Recorded on #542 (closed) and backlog analytics-platform-decision (resolved); #561 (the first-party stay-on-Vercel draft) closed superseded; #385 and #528 trued up. Implementation is a follow-up epic/PR sequence.
- (moment) Economy-canon party (Mary, Samus, Cloud, Link, John, Paige): owner's SixSeven save ($333M, 4-star Classic, 459 hotel rooms banking ~$1.05M/night vs 886 offices at ~$85k/day) proved the pricing-split GDD's own predicted risk ('Classic playthrough sanity pass on early-hotel cash'). Mary's web sweep corroborated ALL canon dollar ladders via the GameFAQs lineage (BStuart/furdude2/Aristotle47): office 2/5/10/15k quarterly, hotel 500-3000/800-4500/1500-9000 nightly, condo 150k. The RO single-source tables are now two-lineage.
- (decision) RATIFIED (owner pre-authorized 'please'): Classic collects the FULL 1994 office rent lump each 3-day canon quarter, killing the 1/30 income-invariant rescale for Classic ONLY (its premise 'we do not have the canon numbers' went stale the day the canon ladders shipped). Seam: GameRules.quarterlyRentScale(quarterDays); Modern keeps the invariant rescale (its calendar toggle must never change per-day income), rule-less contexts fall back to MODERN_RULES. Samus's line carried the room: Classic's contract is parity, not challenge; the money fountain IS 1994, difficulty lives in Modern. DEFERRED with rows: commercial daily-income canon calibration (FAQ says fastFood 3k/day, restaurant 6k/day vs our 2k/4k headline, but our demand-pool scaling makes a bare swap wrong), canon maintenance dollar table (unverified; 1/10 rescale stays meanwhile).
- (outcome) Economy-canon shipped as PR #574 (v1.78.0): quarterlyRentScale seam, full canon lump for Classic, Modern untouched (its golden-master hash held while Classic re-pinned, the seam proof). gds-code-review: Blind Hunter + Acceptance Auditor completed (constants-vs-constants test assertion and an overclaiming comment patched into production-measured collectRent assertions; 3 dismissals recorded in the spec §5b); the Edge Case Hunter run was stopped by the owner mid-flight and did not report. Copilot nits (param naming, copy grammar) fixed and threads resolved. Deferred rows live as #572/#573. Owner flipped the PR ready-for-review himself and called the mid-flight rebase over the memlog collision.
- (moment) Owner's review on #574 landed the epistemics lesson: 'mirrors or descendants of the same source are not automatically independent corroboration.' Mary's 'two-lineage' claim was snippet-level, same-lineage; softened throughout (SOFT tiers stand, everything marked provisional), and the definitive check got its own row: classic-rent-primary-source-verification (#575), Wine harness as the primary path. The 30x cadence change itself stands on the owner's call, not on the snippets.
- (outcome) Follow-ups party (owner: 'go for followups'): research recovered the 1994 commercial chart (fastFood (-3k)-2k-5k, restaurant 4k-6k-10k, shop 4k-20k by popularity, theater 0-2k-10k, party hall 20k flat) AND located the primary sources on archive.org (PC manual full OCR + Prima guide scans; network policy blocks reading, existence confirmed, 'manual unfetchable' premise now stale). Rulings applying the owner's epistemics lesson: #572 SHIPPED provisional (commercialDailyIncome seam, Classic = chart top-tier ceilings as sold-out caps, demand fraction makes the lower tiers; capacity and ceiling are ONE seam so conservation holds; Modern untouched, golden-master proof again), #573 DEFERRED on intra-lineage conflicts (recycling qtr-vs-yr, elevator per-shaft-vs-car, medical unrecovered, star-tiered lobby fees = new mechanic), #575 progressed with concrete source pointers. v1.80.0 (main took 1.79.0 for the Modern empty-lot release mid-flight).
- (outcome) Desktop refusal-tooltip + pan party (Sally UX, Samus, walk-on Rex the desktop Player Advocate): 'Can't build here' card was anchored ON the invalid-preview row, hiding the red strip it explains. Decision: anchor the refusal card one floor-row BELOW the preview strip (caption-below), plus 0.85 opacity on hover-capable devices only as clamp-collision insurance (mobile pinned card stays opaque); Sally capped transparency to protect the dialog grammar ('a tooltip you can't read is a different bug with the same severity'). Shift+left-drag = pan for every tool, deliberately beating elevator drag-sizing and floor painting; help lede mouse line rewritten (space-drag was never documented), keyboard Shift-x10 line untouched. Deferred to backlog: grab-hand cursor while Shift held. Minor version bump.
- (moment) Rex turned up (desktop player advocate, buzz cut, sandwich-hand spacebar gripe, 'I file good tickets'). Wanted 60% glass; Sally held the line at 0.85-plus-placement. His exit line 'Caption goes UNDER the whiteboard. Wild concept.' is a keeper callback.
- (outcome) Shift-pan/refusal-card review round: gds-code-review's three layers confirmed the party design but caught 6 patchables, the two sharpest being Shift+wheel ratcheting zoom-out-only (browsers remap Shift+wheel to deltaX) and the extend-arrow hit-test running ahead of gesture classification so Shift+press on an arrow resized the shaft. All fixed on branch; hybrid-touch tap-paint under a held modifier deferred (#589, Space parity). Sally's AC6 catch stood: the first cut only appended the truthful sentence while leaving the flat 'drag to pan' lie in front. Shipped as PR #590, v1.82.0.
- (outcome) Correction to the Shift-pan ship record: PR #590 released as v1.84.0, not v1.82.0 (main took 1.82.0 for the Food Hall and 1.83.0 for gallery UX mid-flight, and the branch re-minted on merge). Round-2 review (4 layers, post-merge) caught the ungated wheel deltaX fallback (unmodified sideways scroll zoomed) plus doc/CSS-placement polish; shipped as the v1.84.1 follow-up PR.
- (outcome) Splash Load-Tower party (Sally, John, Samus, Link, + summoned Grumbal/Boundary, walk-on Mo the phone player). Question was slot-dialog-vs-file-picker for a new splash action; room struck the binary. LANDING: ONE 'Load Tower' action -> ONE load-only surface listing device slots (auto+1-3) WITH 'Load from a file...' as a row in the same list. Grumbal's kill on reusing showSaves: saves.ts rows carry Save/Delete + footer Export, which on the splash would write/export the THROWAWAY BOOT SIM (main.ts:198) and break the standing 'never mutate the sim while the splash is up' invariant (saveLoad.ts saveBeforeUpdate comment; the 30s autosave timer already checks for #splash). saveToSlot would not even throw, it would silently succeed. Boundary killed file-picker-only: slot saves are in localStorage, invisible to any OS picker, and .vctower needs the application/octet-stream accept hack on Android. Real hole this closes: hadReadableSave (appBoot.ts:283) reads ONLY the autosave keys, so a corrupt autosave beside a healthy slot 2 leaves the player no route to their tower except founding one they do not want.
- (decision) Splash Load-Tower supporting rulings: order Continue / Load Tower / New Tower (Sally: New Tower is the only action that can COST you something, so it sits furthest from the default focus; .primary styling when no save is unchanged). Label says 'Tower' not 'Save' (Mo: 'I think of it as my building'). Raw presence (hasSlot) not parse-based listSlots().exists, so a corrupt-but-present slot is SHOWN and labeled unloadable rather than hidden (Boundary, echoing preserveUnreadable's own reasoning); that row is the strongest cue for the file row beneath it. Empty state = one honest line plus the file row, never four 'empty' rows on a phone. John's one-line invariant, which Sally called the whole spec: THE TITLE SCREEN IS DISMISSED BY A TOWER ARRIVING, NEVER BY A DIALOG OPENING (so: no teardown on open, on OS-picker cancel, or on a failed/corrupt load; teardown on successful adoption only, then setSpeed(0) + 'press play to resume' exactly like onContinue). Implementer notes: Onboarding.ts:240 focus fallback and screenshot-page-ops.ts:232 both select [data-splash=continue]/[data-splash=new] and must keep working; the focus-trap tab-order test asserting mute+install are last two must still hold.
- (outcome) Splash Load-Tower RATIFIED (owner sign-off, 2026-07-28): the room's landing carries in full (one Load Tower action -> one load-only surface with slots AND a 'Load from a file...' row), and Sally's ordering carries too: Continue / Load Tower / New Tower, destructive action furthest from default focus. Grumbal's framing accepted for the record: this is a bug fix with a button attached, not a feature (hadReadableSave reads only the autosave keys, so manual slots are unreachable from the title screen today). Next: gds-ux for the screen design, then bmad-spec for the CAP contract (precedent: spec-splash-mute and spec-pwa-install, whose CAP ids splashTemplate.ts cites inline).
- (outcome) Invisible-refusal party (#658, follow-up to the merged Load Tower PR #655). Sally, Link, John, Grumbal, Boundary, walk-on Mo whose bug it literally is. The defect: showImportReport/showExportReport guard on isModalOpen() and report the refusal with a toast, but a <dialog> renders in the browser TOP LAYER and paints over the toast rail at any z-index, so that message is unreadable BY CONSTRUCTION, only ever firing when something is guaranteed to cover it. Boundary's reframe carried the room and Grumbal conceded it was better than his own: isModalOpen() is one boolean standing in for two different facts, 'a modal is on screen' vs 'a modal owns a pending decision', and the code has only ever had the first.
- (decision) REJECTED displace-unconditionally: openModalTemplate does replaceChildren(), so displacing showEventChoice ERASES it mid-flight and its fire-once onResolve never runs while frameLoop.ts:44 holds the sim frozen on shownChoice. Deadlock, no dialog, no way out. REJECTED unbounded queue: the import report closes over a fully parsed Simulation, so held across a tower swap or a second import it eventually adopts the WRONG tower, or ambushes a player who moved on (Mo: 'if I'm three taps into Settings and a 1994 import report jumps out at me, I'm going to think I broke something'). REJECTED refuse-but-visible alone: honest and the safest single change, but Link's point killed it as the whole answer, we already HOLD the parsed bytes by the time we refuse, so making the player re-pick from cloud storage throws away completed work. Sally's line: we are not missing data, we are discarding a successful parse because a window was in the way.
- (decision) RATIFIED (owner sign-off): split the guard into modal-open vs modal-owns-a-pending-decision. Event choice and the update prompt are blocking; Help/Settings/Saves/tower-picker/congrats are dismissible. Over a DISMISSIBLE modal the report TAKES THE SLOT (owner picked this over wait-until-closed and ask-in-place): those dialogs hold no pending state and the player already asked for the import by picking the file. Over a BLOCKING modal it waits on a short leash tied to that specific dialog resolving. If the leash breaks (tower swap, second import, resolves elsewhere) the parsed tower is DROPPED and the player is TOLD plainly somewhere visible (owner picked this over holding a Simulation in memory behind a new re-entry chip, and over dropping silently, which would just reproduce the defect at a different moment). Covers export too: showExportReport has the identical guard and the identical invisible toast.
- (moment) Grumbal's binding testing rule, seconded by Boundary: a test must assert the PLAYER IS INFORMED, not that a code branch was taken. 'We called toast()' is not the same claim as 'the player was told', and this bug is the proof, since it shipped precisely because the failure is only reachable from the state that conceals it.
- (outcome) Sticky-dialog-footer party (Sally, Link, Grumbal, Boundary, walk-on Rex; owner-prompted after he lost elevator-schedule edits). Diagnosis: .modal-box is max-height:82vh with overflow-y:auto, .modal-box > .win-title is already position:sticky, but .modal-actions never was, so on a tall dialog (the 24-hour schedule grid) Save/Cancel scroll below the fold. Worse than forgot-to-save: the dirty guard's ONLY feedback is the Cancel button relabelling to 'Discard changes?', and Esc preventDefaults the close, so the player presses Esc, sees nothing happen, presses again, and the second press discards. Two keystrokes, warning off-screen both times. Grumbal: the same disease as #658, a confirmation rendered where the player cannot see it.
- (decision) RATIFIED (owner): .modal-actions goes sticky on ALL modals, which is self-scoping because position:sticky;bottom is a no-op on a box that does not overflow, so only genuinely scrolling dialogs change pixels (Grumbal withdrew his gallery-churn objection on that mechanic). Plus the armed state announces itself as WARNING TEXT IN THE PINNED STRIP ('You have unsaved changes' beside Discard) rather than the easily-missed label swap, because Esc leaves the player looking at the dialog, not the button. Rejected save-on-close (silently commits experiments). SEQUENCING: its own PR and it goes BEFORE #658, since this is data loss the owner has actually hit while #658 is a P3 race nobody is losing towers to today.
- (outcome) Form-library side question (owner asked; Winston summoned to argue the pro side, and conceded). Answer: NO library. Decisive fact from Boundary/Link: this bug was never a state bug, dirty/cancelArmed/preventDefault all worked perfectly and the failure was a missing position:sticky, so no form library would have caught it. Supporting: 7 runtime deps total and lit-html WITHOUT LitElement, so there is no component base to attach to (React Hook Form/Formik/Final Form/VeeValidate are framework-bound; TanStack Form and Felte ship agnostic cores but expect an adapter this project lacks); bundle discipline is a standing rule (the project built a seam specifically to avoid shipping a ~50KB analytics SDK); and Sally's count, two of the four dialogs are not forms at all (schedule is a recomputing 24xN toggle grid, batch pricing is a preview), while the two that are form-shaped have never produced a bug.
- (decision) What IS real from the form-library question (Winston's salvage, Boundary called it the strongest argument in the room and it argues AGAINST the library): the duplicated concept is not validation, it is the DISMISSAL CONTRACT, what happens when a dialog holding unsaved work is closed by button, Esc, the title-bar x, or a backdrop click. The schedule dialog is the only one that covers all four paths and needed an explicit comment to get there. That wants a small internal helper we own, not a dependency, and it is the SAME seam #658 needs, since that fix must also know which dialogs own pending work. Two bugs, one missing abstraction. Owner not yet answered whether the helper lands in the sticky-footer PR or stays out of it.
- (outcome) S6 party (Vercel-report retirement): room voted retire, nobody defended Vercel Web Analytics. John's gate: parity numbers from run #23 written into the epic BEFORE deletion. Real S6 scope flagged by Amelia: adapter dual-write + @vercel/analytics import, not just the report script; gds review + patch bump. Paige: transparency note ships same PR. Speed Insights: room leans keep (CWV has no PostHog replacement), John demands the call be recorded either way. Winston: in-flight window-parameter PR #692 polishes the doomed script; recommended slimming it to PostHog-only. Victor's first appearance -- 'fire the analytics, keep the stopwatch' landed; reads epics upside down. Owner decision pending on PR slimming + S6 go.
- (outcome) Page-views round (owner asked mid-S6): unanimous keep, inject-only. Mary's turnstile framing (referrers/UTM = the one acquisition signal cookieless still gives); John tied it to store listings and launch posts; Winston's option A (inject-only, 20-line diff) beat Victor's one-stack $pageview relay (option C, real story). Victor conceded WITH a written exit clause: Vercel gets page-level plumbing only, zero events, and if Vercel ever gates the turnstile, relay $pageview is the pre-agreed exit. New Victor bits: 'fire the analytics, keep the stopwatch... and the turnstile', 'I contain multitudes. Mostly exits.' Paige verified the just-shipped Help copy stays literally true (beacon is same-origin, cookieless) and got her two-word widening (page-visit counts). Boundary now in SPEC: events-versus-pages, not vendor-versus-vendor.
- (outcome) #699 gripe-copy ruling: the congestion gripe goes SERVING-AWARE, one classifier (passenger transports that stopsAt the unit's floor, staff-only excluded, skipFloors honored) shared by inspector line and churn buy-back toast. Any stopping passenger elevator -> keep 'crowded elevators. Add cars or a parallel shaft to this block.'; walkways only -> name what is there (crowded stairs / crowded escalators / both) with remedy 'add another stair column (or escalator) or give this floor an elevator stop'; empty-list fallback = transport-neutral wording. Stair crowding STAYS in the model (8/trip stands): realistic (stadium/subway stairwells) and the balance lever until #384 willingness enforcement lands. Scope capped at three strings: gripeCopy.ts:21, churn.ts:104, index.html elevator-traffic tooltip title; no engine math change, patch bump.
- (dynamic) Boundary's seam bit x6: 'the copy read the MODEL, the model read the TOWER, nobody read skipFloors' - the classifier must route through stopsAt, never bottom/top spans, or a skipping shaft flips the wording back. Samus defended stair crowding as canon-adjacent game feel ('the 4-flight refusal is the 1994 lever, #384 owns it, do not make stairs uncrowdable'); John capped the sweep at three strings ('who asked for a copy audit?'); Link claimed one engine helper in sim/gripe.ts so churn and the inspector can never disagree.
- (outcome) #699 shipped as PR #700 (v2.4.1): ruling implemented spec-first (story 699-1), red-green, all gates green. gds-code-review triage: 5 patches applied (comment honesty, fallback test, gate-verdict precondition, fixture asserts, one X-not-Y comment), 3 dismissed (Blind Hunter's operational-state finding refuted: Transport has no build state), 1 defer -> curated row congestion-gripe-binding-shaft / issue #701 (name the BINDING shaft, kind-priority + segment facets; needs loadByShaft exposure). Boundary's seam count stands at x6.
- (outcome) #701 ruling (binding-shaft congestion copy): the congestion builder grows a per-floor BINDING attribution (worst serving shaft) exposed beside the ratios via one shared internal build (spatialCongestionByFloor stays a thin projection, math byte-identical). Copy names the binding shaft's KIND. Tie rule (Boundary): proportional split makes single-floor ratios near-equal by construction, so a walkway flips the wording only when STRICTLY worse than every serving elevator beyond 1e-9; ties keep v2.4.1 behavior (elevator wins; tied walkways keep the combined stairs-and-escalators line). New mixed-floor copy when a walkway binds beside a stopping elevator: 'crowded stairs. They also carry floors no elevator stops at; give those floors an elevator stop, or add another stair column.' v1 scalar + missing-entry floors fall back to the v2.4.1 kinds classifier. Segment facet DISSOLVED by construction (Cloud): the copy now relays the model's own attribution, so segment honesty arrives if and when the model goes segment-aware, a model story, not copy work; #701 closes fully with this PR. Row -> resolved on merge. v2.4.2 patch.
- (dynamic) Boundary x7: 'your ties are not rare, they are the default; proportional split means every single-floor bank is a photo finish' forced the epsilon-strict flip rule. Grumbal tried to kill the whole story as gold-plating ('#699 shipped, who is paging you?'); Samus countered with the reporter-empathy read (the mixed-floor player gets told to buy cars that will not help) and John let it through as a P3 rider sized under a day. Cloud's 'the copy relays the model, it does not out-model the model' line dissolved facet 2 and closed the scope argument.
- (outcome) Peek follow-ups party (Sally, Samus, Cloud, Link, John, summoned Grumbal/Boundary, Mo the phone player back for the feature that is literally his thumb; owner pre-authorized 'please'). RULING 1: #634 progress-ring affordance WON'T-SHIP. The defer condition ('implement if the hold reads as lag on device') resolved negative on the owner's device test; iOS/Android long-press convention is a cue AT the knee not during the hold, and our card appearing IS that cue (Sally: 'that's not affordance, that's costume'); Cloud flagged reduce-motion as a mandatory second path for zero complaints. Samus's flag: if playtesting ever surfaces 'didn't know I could hold', the remedy is teaching copy, not a ring. Grumbal killed something and the room helped; he asked for it on the calendar. RULING 2: #696 dismissal latch: SPEND IT. resetLatch's own comment already ruled ('an explicit tap/click is fresh intent'); a 450ms stationary hold is more explicit than a tap, and the latch exists to stop hover jitter, the opposite of a hold. Peek path routes through the SAME resetLatch seam selectPicked uses (one intent gate, two callers). Pure touch untouched (peek card has no ✕); vanished-unit-mid-hold swallow stays (absorbing-state invariant, not this bug). Patch 2.6.1, no changelog line (2.4.1 precedent), story-spec first, /bmad-code-review.
- (dynamic) Boundary's seam bit hit x8 on the latch: 'two entry points into show-this-card, and only one of them knew the latch existed.' Mo's line for the record: on his mom's Surface the swallowed hold 'reads as the game froze.' Link rated the fix 'boss fight rating: tutorial slime.'
- (outcome) Peek follow-ups shipped as PR #704 (v2.6.1, merged same day): the latch-spend landed exactly as ruled (one resetLatch call through the selectPicked seam, order-pinned test), #696 closed completed, #634 closed not-planned with the ruling on the issue and GDD section 13. bmad-code-review triage: six patches, the sharpest being the story's own pre-filled review record (the Acceptance Auditor caught the scribe minting the minutes before the meeting; rewritten from the real output), a vacuous re-raise assertion, and the unpinned reset-before-pick ordering both hunters flagged independently. No defers. Outside bots stayed down (Codex credits, Copilot silent); two confirming passes carried the load.
- (outcome) #548 demand-calibration party, two rounds (inline: Samus, Mary, Link, John, Grumbal; then a REAL independent round of four who checked the code). The independent round corrected the inline room three ways: Mary found the "leave bar 0.4" was wrong (notice fires at satisfaction 0; 0.4 is VACATE_RESCIND), turning "7 hours" into ~20 hours plus 2-day grace; Samus proved the 0.08/0.30 pair parks a share-9x tower at its cap forever (erosion 0.0504 vs recovery 0.05, a break-even sold as drama); Mary inverted the 12-Apartment example (share ~0.16, retail-overbuilt, not share 2). Link probed the Modern golden empirically: coverage 1.0 throughout, no re-pin needed, no test verdicts flip; blast radius adds the backlog deferral-inbox bullet plus four PROVISIONAL notes. Grumbal found the synchronized-cohort crater (tower-uniform coverage marches every tenant down in lockstep on save load) and the attribution lie (unmet erosion above 0.07 outranks noise in the max while dominantGripe names noise first: "the player soundproofs a building that's dying of no shops"), conceded the gate-starves-shops deadlock is backwards (shedding self-limits) and demanded the serialized share-12 five-day test with buy-back and cause-histogram asserts.
- (decision) RATIFIED (owner "ok"): unmetDemandErosion 0.055 -> 0.12, UNMET_DEMAND_EVICT_FLOOR 0.25 -> 0.35 (net shed below coverage ~0.204, share ~4.9x; one game day of visible decline at 9x), WITH the dominantGripe attribution fix in scope (name unmet demand ahead of noise when it is the binding steeper drain). Locked: Grumbal's five-day serialized-tower regression, boundary pins in gameRules.test.ts, backlog bullet and PROVISIONAL notes rewritten, attendance-in-coverage row filed P1, perCapita retune stays telemetry-gated in #548, pairwise reachability its own story, Classic cap-only untouched, minor bump.
- (dynamic) The round's epistemics lesson echoed #574: the inline room's numbers were confident and wrong until four independent minds recomputed them. Samus's counter-pair carried on pacing grounds (noisy-neighbor precedent ~-0.02/hr); Grumbal's exit line stands as the testing bar: "the room says it's fair" is not a number.
- (event) Party (2026-07-29, autonomous, owner asleep) verified GDD gdd-ground-floor-lobby-only-2026-07-29: VERDICT approve-with-changes. Cast: Game Designer, Game Architect, Adversary/QA, UX.
- (decision) Ground-floor lobby-only: surprise-spend guard is PRE-COMMIT: Floor tool on floor 1 must render a lobby ghost and lobby price (/tile summed over the drag) before the click; toast is secondary and goes through the aria-live region. Modal confirm rejected for v1 (would punish founding click); threshold confirm on big spends is a tracked follow-up.
- (decision) Coerce floor->lobby on floor 1 in ONE seam so canPlace/place/preview/cost/founding agree; convert BEFORE founding validation (else the beginner Floor-tool click is rejected as 'not a lobby'). Sky-lobby asymmetry (floor 1 auto-converts, claimed sky lobby refuses) is intentional and must be documented at the seam.
- (decision) Confirmed: charge lobby price, all-or-nothing affordability, floor-1-only scope, no save migration. Edge cases to test: already-lobby tile no-op, drag straddling lobby charges only empty tiles, legacy bare-floor-1 save loads.
- (event) Party (2026-07-30, autonomous) on extending floor-1 auto-convert to SKY-lobby stories: VERDICT ratify-with-changes, GATED on live build-confirmation before merge (reverses the gdd-ground-floor-lobby-only sky-lobby scoping).
- (decision) Sky-lobby auto-convert design ratified: Floor tool coerces to lobby on ALL lobby stories (1 + 15/30/45/60/75/90). Rooms on story 15 are UNAFFECTED (room tools lay substrate via the primitive, not the coerced Floor path), so 'conditionally lobby-only' survives: a sky story can be a room floor OR a sky lobby, never bare floor. Reuse the floor-1 money guard (lobby ghost, price, red-when-unaffordable, toast). Coerce BEFORE the sky-lobby refusal checks so the floor-refusal branch (placement.ts:110-112) goes dead; the 'clear first' message survives ONLY for rooms (a lobby can't sit on a room). No save migration (legacy bare-floor-on-15 kept on load).
- (decision) GATE (Grumbal, seconded by all): the change reverses a ratified decision AND edits canon on INDIRECT evidence (palette grouping + render-proof from tdt-format 2026-07-19). The missing piece is BUILD-proof: nobody has watched a Floor drag on story 15 turn into a lobby in the retail game (the automated placement did not register; one run crashed). Do NOT merge until a human or a non-crashing harness run witnesses it. Implement behind the seam meanwhile; hold the merge on the confirmation.
- (event) REAL AGENT PARTY (2026-07-30, 4 independent agents w/ repo access) on extending auto-convert to sky lobbies: broad proposal (Floor tool->lobby on 15/30/...) REJECTED (Designer reject, QA reject, Canon gate-on-live, Architect reject-part1). Corrects the earlier inline-party ratify-with-changes: the earlier read over-generalized floor-1 (UNCONDITIONALLY lobby) to sky stories (only CONDITIONALLY/optionally lobby; commonly plain office floors).
- (decision) RATIFIED narrow fix (Architect, addresses the owner's original 'lobby on top of floor'): do NOT coerce the Floor tool on sky stories (keep placement.ts:110-112, keep groundFloorStructureKind floor-1-only). Instead relax the sky-lobby whole-floor gate at placement.ts:113 from floorHasNonLobbyContent (any non-lobby) to floorHasRoom (rooms only), so the LOBBY tool upgrades bare floor in place on a sky story (the 116-126 upgrade path already works), refusing only rooms. Keep it a WHOLE-FLOOR rooms gate (not span-local) to preserve the uniform-concourse invariant. Add an O(1) floorHasRoom counter mirroring nonLobbyTiles. No coercion => sim/build.ts, buildPreview.ts, buildActions.ts, ui/placement.ts untouched. No import-path change (Canon guardrail). No live-game gate needed (engine-local, headless-testable).
- (decision) Tests: flip Tower.test.ts ~97 (sky lobby over bare floor now UPGRADES, not refused); update simulation.integration sky-lobby copy (rooms are the blocker, not bare floor); add: lobby upgrades bare floor on 15 (no rooms) ok; lobby on 15 with a room refused (rooms-only reason); Floor tool STILL lays bare floor on 15 (no coercion) + office drops on it; floorHasRoom correct across multi-story footprints/removal/reindex; claimed sky lobby still refuses rooms; 15/30/45/60/75/90 covered, 14/16/29 unaffected; legacy mixed lobby+floor story becomes finishable on load. Player-facing minor bump.
- (event) Human-audio-theme party (Samus, Sally, Cloud, Link, Grumbal, John cameo) reviewing spec-human-audio-theme: full replacement of composed music + jingles with the owner's recorded package (S1 splash, Two Chapters bed, F4 Terrace Peak fanfare, bloop/ping SFX family), superseding PR #776
- (outcome) RATIFIED with five refinements, none touching owner-approved note data: (1) dedicated percGain node into musicBus, ramped alongside musicGain in crossfadeProgram (else the heartbeat thumps alone through the crossfade dip); (2) TWO mono MembraneSynths, thump tuned low and tap tuned high, both into percGain, both in the dispose list (one shared synth cannot voice both); (3) error cue retrigger guard ~400 ms (two-bloop gesture on a mono ramped voice is not spam-safe like the old poly note); (4) bed melody velocities scaled 0.8 vs the audition previews (auditioned in isolation, plays under crowd din; splash stays bright); (5) the graph test must assert perc BYPASSES musicTone/musicSub, not merely that it connects
- (note) Owner-vetoable implementation note (Samus): the TOWER win strikes the F4 phrase twice, second pass an octave up, so the win outranks a star promotion while sharing the cue
- (dynamic) Samus opened on the retired carillon (her #776 hill) and conceded to Sally's leitmotif argument: the splash sings the phrase, milestones ring it back. Grumbal cut the fanfare-contour debate short as a settled owner decision and spent his three findings on the mono error voice, bed mix fatigue, and decorative graph tests. John's cameo: the changelog line IS the headline (whole soundscape human-made from the owner's recordings)
- (outcome) Owner vetoed Samus's win-variant (F4 twice, octave up): win and star promotion share the identical plain F4 phrase. Samus's hill count: 0 for 2 this package
