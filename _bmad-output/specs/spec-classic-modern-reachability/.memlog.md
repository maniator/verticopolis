---
topic: In-app Classic vs Modern ruleset reachability (shared compare template, in-game mode badge + compare modal, founding expand-in-place)
updated: 2026-07-21T18:55
---

- (direction) Owner asked a 5-agent party (UX, visual/craft, game design, architecture, dev) to design ruleset reachability before spec'ing; party consensus recorded below.
- (capability) CAP-1 single canonical comparison: extract the Classic-vs-Modern body from help.ts into src/ui/templates/compare.ts (compareTemplate); every surface imports it. Success: help.ts renders it inside the unchanged Classic-vs-Modern <details> and help.test.ts drift guard passes with NO edits.
- (capability) CAP-2 active-mode label in-game: a Tower-panel badge reads 'This tower: Classic'/'This tower: Modern' from the live mode. Success: badge reflects ui.cb.getMode()/sim.mode, set from uiStatus after setTowerName, updates on adoptSim, no-ops when unchanged.
- (capability) CAP-3 reach the comparison in-game: clicking the mode badge opens a Compare modal. Success: uiDialogs.showCompare opens compareModalTemplate (h2 + compareTemplate + close) via the single #modal; closes cleanly.
- (capability) CAP-4 founding expand-in-place: a COLLAPSED <details class='nt-compare well'> under the two mode cards reveals compareTemplate inline. Success: newTower renders it collapsed, radio state untouched (one DOM box), no second surface, no newTowerModal wiring change.
- (decision) Reachability is the primary value (game designer): a returning player cannot tell which rule-set the active tower runs, nor look up what Modern does; CAP-2/CAP-3 close that. Founding (CAP-4) is secondary clarity.
- (constraint) In-app only: reuse the single reused #modal (UI.ts openModalTemplate) and lit-html. No standalone HTML page, no client-side router, no Vite input / Workbox precache / Vercel change.
- (constraint) No modal stacking and no drawer: showModal() is browser top-layer, so a drawer renders behind the backdrop, and a second dialog occludes the first on mobile and compounds the backdrop; both fail. Founding uses inline <details> only.
- (constraint) compareTemplate is the ONLY copy of the comparison prose. help.test.ts (RULE_TO_HELP map + copy-sync + uniqueness, found via sectionBySummary('Classic vs Modern')) must pass UNEDITED because compareTemplate renders inside that same section.
- (constraint) Parity-pride: no 'Modern adds N' count on the founding screen; frame Classic as the faithful reference build, never as fewer features. Keep the existing 3-feature 'Modern adds' teaser.
- (constraint) Do NOT render mode inside #tower-stats (lit one-renderer invariant); the mode badge is a separate element. setMode updates label/class only when changed (no per-pump cost).
- (constraint) House style: American English, no em-dashes, no marketing words, no 'X, not Y' pattern. Minor version bump (player-facing). Review lane /bmad-code-review. Do not touch GameRules/engine.
- (decision) Rejected standalone /help.html: duplicates canonical prose (drift), needs a Vite input + Workbox precache entry + Vercel navigateFallback carve-out, near-zero SEO/share value for a founded-tower reference.
- (decision) Rejected client-side router: one canvas view, nothing to route; a hash route is the lightest future deep-link but is not needed now.
- (note) Tests: new compare.test.ts (all divergence phrases + 'pixel-faithful to 1994'); newTower.test.ts (collapsed .nt-compare, summary text, a signature phrase); help.test.ts unchanged+green; uiStatus.test.ts (#btn-mode label/class follows sim.mode, no-op unchanged); uiDialogs integration (showCompare open/close; #btn-mode click opens it, add button to the DOM fixture).
- (event) Spec derived; self-validate pending.
- (event) Self-validate: Pass 1 coherence OK (4 caps each intent+success, constraints bend design, non-goals explicit, success signal testable, lean). Pass 2 preservation OK (single-source, badge, compare modal, founding details, rejected page/router/drawer/stacking all landed; test list in memlog). One open question: review lane bmad vs also gds.
- (direction) Owner expanded scope: a SHAREABLE/linkable help page (open in new tab, target=_blank), screenshots/GIFs demoing Classic vs Modern on it, and a reusable RETRO page design for future pages. Full party reconvened.
- (correction) Copilot fix to first spec: mode source is ui.cb.getMode() (=app.getSim().mode in src/game/uiCallbacks.ts), NOT sim.mode in uiStatus.ts. Badge update is behavioral/dirty-gated (no DOM write when unchanged); setMode is a NEW helper, not existing. Drift helper is sectionBySummary(frag,'Classic vs Modern'); the summary wraps text in a nested <span role=heading>.
- (decision) Interaction (ux+game): ONE system, two presentations. In-app MODAL for the mid-game quick check (mode badge + founding reference) keeps the player in the live sim; the standalone /help page is the shareable canonical. target=_blank only on an explicit 'Open full page' link inside the modal.
- (decision) Founding: expand-in-place collapsed <details> (shared comparison), not new-tab. Mode badge opens the modal (status-label click = 'tell me more').
- (decision) Standalone /help page in scope. Add src/help.html as a Vite input; PRECACHED automatically (Workbox globPatterns matches .html) as long as NOT globIgnored; add /help to navigateFallbackDenylist; page does NOT registerSW (root-scope game SW covers /help).
- (decision) Vercel: targeted rewrite {/help -> /help.html}, NOT sitewide cleanUrls (would break gallery canonical). rel=canonical /help + sitemap url.
- (decision) target=_blank reality: fine in a browser tab; Android/desktop standalone breaks to system browser; iOS standalone unreliable. Progressive enhancement: real <a target=_blank rel=noopener>, but a click handler feature-detects standalone (matchMedia('(display-mode: standalone)')||navigator.standalone) and opens the in-app modal instead when installed.
- (decision) Pause behavior: a modal does NOT auto-pause the sim today (engine.paused only via setSpeed(0)). DECISION: the in-app compare/help modal should pause the tower on open (setSpeed(0)) and restore the prior speed on close, so reading the reference never costs the player. The new-tab /help case is a backgrounded tab the browser throttles; on return the game resumes at its prior speed. 'Back to game' from the modal = close it; from the standalone page = a link to / (for someone who arrived via a shared /help link with no running game).
- (decision) Single source: src/ui/templates/compare.ts compareTemplate() rendered by the Help modal, the compare modal, the founding <details>, and the standalone page. help.test.ts drift guard passes unedited.
- (decision) Retro design system (visual): extract :root tokens (styles.css ~22-64) -> src/styles/retro-tokens.css and .win/.win-title/.well/.field/.btn/.kv -> src/styles/retro-components.css; styles.css @imports both (app unchanged). New src/styles/retro-page.css imports the same two. pageShell(title, backHref, main) template helper. Dark theme in the page layer. Retire gallery.html's duplicated palette; test that no page HTML redeclares --r-face.
- (decision) Media: NO GIF tooling exists; GIFs a NON-GOAL for v1. Paired Classic/Modern STILLS from the pinned Playwright container via new scripts/scenes/classic-vs-modern.ts (fixed seeds, fail-closed asserts, docs/screenshots/features/), riding the existing drift gate; no new gate. CSS crossfade of two PNGs on-page if motion ever needed.
- (decision) Screenshot shortlist: reuse existing paired stills (mode picker, elevator schedule, pricing, stats tenancy); ADD escalator-on-office + hover refusal-reason (clearest unshot); optional 'livelier day' still at a fixed hour. Caption-only for data/math diffs (relocation, overhead/tax, churn, cockroach recovery, express routing).
- (decision) Phase it (epic). P1: shared compareTemplate + retro design-system extraction + /help page (SEO/precache/Vercel/standalone-fallback) + in-game mode badge + compare modal (pause-on-open) + founding expand-in-place. P2: paired Classic/Modern screenshots scene + wiring into the page. Review lane /bmad-code-review.
- (decision) Rejected: client-side router, Workbox runtimeCaching, self-registering SW on the page, sitewide cleanUrls, SSR/prerender, GIF tooling v1, modal stacking, drawers, and a 'Modern adds N' count.
- (decision) Styling approach: reject CSS Modules. The repo styles through one global token-based styles.css and lit-html templates use plain class= string literals (no JSX className rewrite), so Modules would fragment the token model and lose their ergonomic win while duplicating the prefixing convention already in place. The retro design-system extraction (retro-tokens.css + retro-components.css, re-imported by styles.css and retro-page.css) IS the reuse mechanism; CSS Modules is an explicit non-goal. Shadow-DOM static styles noted as the lit-native path if component scoping is ever wanted, out of scope here.
- (direction) Owner folded the sprite gallery into scope: (a) restyle gallery.html onto the SAME retro design system (adopt retro-tokens.css / retro-page.css, retire its inline duplicated palette), so it is a second consumer of the reusable page kit; (b) fix the squished gallery cells so every kind renders at its true aspect; (c) cross-link the pages (Help/compare page links to the Sprite Gallery, and the gallery links back).
- (decision) Gallery restyle (part of CAP-6): gallery.html drops its inline palette copy and imports retro-tokens.css + retro-page.css; its header becomes the shared .win title bar via pageShell (or the same chrome), and it gains a Back-to-game link and a link to /help. This makes retro-tokens.css the single --r-face declaration (the de-dup guard test covers gallery too).
- (decision) Gallery height fix: the fixed CELL_H (170) plus roomEntry aspect-scaling shrinks tall multi-floor kinds (metro 3-floor, cinema/party hall 2-floor) so they read squished next to single-floor rooms. FIX: size cells so each kind renders at its true proportion (raise the cell height and/or key the row height off the tallest kind, keep aspect, keep the canvas grid readable). Success: no cell visibly vertically compressed; multi-floor kinds show full height; the paired screenshot/visual check confirms.
- (decision) Cross-links: the retro pageShell nav carries the sibling links. /help links to the Sprite Gallery; the gallery links to /help and Back to game (/). All plain same-origin anchors so they work cold, no-JS, and installed.
- (decision) Vercel tracking parity: the game injects @vercel/speed-insights + @vercel/analytics in bootstrap.ts, host-gated to verticopolis.com / *.vercel.app (best-effort, try/catch, so 404s stay off localhost/preview/Capacitor). The standalone /help page AND the gallery must inject the same telemetry the same way (a shared helper reused by both entry scripts, same host gate), so every page reports Core Web Vitals + page views like the game. Both pages also carry full SEO/OG head parity: title, description, rel=canonical (/help and /gallery.html), robots, og:*/twitter:* tags, and a sitemap entry, matching index.html/gallery.html conventions. gallery.html gains the analytics it lacks today.
- (decision) Gallery clean URL: add the same targeted Vercel rewrite for the gallery, {/gallery -> /gallery.html}, alongside {/help -> /help.html}. The gallery canonical + sitemap move to the clean /gallery; sibling links point at /gallery (not /gallery.html). Still NO sitewide cleanUrls (two explicit rewrites only), so nothing else changes. Update gallery.html's existing rel=canonical (currently /gallery.html) to /gallery.
- (decision) Deployed-page audit for the cleanup ask: the Vite inputs are src/index.html (game), src/gallery.html (public), src/preview.html. preview.html is the ACTIVE e2e/screenshot harness (referenced by playwright.config.ts, the e2e specs, and scripts/screenshots.ts), so it stays. No orphaned deployed .html page was found to remove; the non-deployed .html files are skill assets and party-mode transcripts, out of scope. If implementation surfaces a genuinely dead page, remove it and its Vite input; otherwise the cleanup is a no-op with this finding recorded.
- (event) Re-derived SPEC.md (8 capabilities, P1 CAP-1..7 / P2 CAP-8) with 3 companions (page-architecture, retro-design-system, media-plan). Self-validate Pass 1 coherence OK: every cap has intent+success, constraints bend design, non-goals explicit incl CSS-Modules/router/cleanUrls/GIF/live-page-removal, success signal concrete, IDs stable CAP-1..8. Pass 2 preservation OK: shared compareTemplate, in-game badge, pause-on-open compare modal, founding details, standalone /help (back-to-game + installed-PWA fallback + precache/rewrite/canonical/sitemap/telemetry), retro kit (single --r-face + pageShell + dark theme), gallery (restyle + height fix + /gallery rewrite + telemetry + cross-links), paired stills P2 + caption-only + GIF non-goal, preview.html-stays audit all landed. Open Q: review lane bmad vs also gds; exact gallery cell sizing left to impl.
- (correction) SPEC.md drift-guard citation corrected to the real two-arg call sectionBySummary(frag, 'Classic vs Modern'), matching on the summary text carried in its nested <span role=heading> (Copilot low-confidence nit on SPEC.md:24).
- (decision) Screenshot impact called out for P1 (not just CAP-8's new scene): the mode badge in the Tower panel and the gallery restyle + cell-height fix render new pixels, so existing captures (main/tower-panel scenes; gallery visual snapshot in e2e/visual.spec.ts + milestones.spec.ts) drift and must be regenerated via the pinned container. Help '02-help' scene is captured collapsed so it should not drift (verify at impl). /help is new, no prior capture. Recorded in media-plan.md + CAP-7 success.
- (correction) Second Copilot pass on 0e7c033, 4 accuracy fixes: (1) retro-design-system 'byte-for-byte unchanged' -> computed styles/render unchanged (extraction moves rules between files); (2) media-plan header CAP-7 -> CAP-8 (paired stills are CAP-8; CAP-7 gallery restyle also cites the companion for its screenshot-impact note); (3) page-architecture precache row no longer mislabels gallery.html as a 'tooling page' -- it is a public page kept out of precache as a deliberate size choice (low offline value), distinct from preview.html the non-deployed harness, promotable later; (4) SPEC CAP-1 wrapper snippet now shows the nested <span role=heading aria-level=3> so the a11y heading semantics are not dropped during extraction.
- (correction) Copilot (correct, substantive): Workbox generateSW precaches the built /help.html, NOT the clean /help. Adding /help to navigateFallbackDenylist stops index.html hijacking it but leaves offline /help network-only. FIX: register the clean /help as a precache alias for help.html (generateSW additionalManifestEntries / manifestTransform, keep generateSW, no injectManifest) so the clean URL resolves from cache offline. /gallery gets no alias (gallery intentionally not precached, offline /gallery unsupported by design). Spec CAP-5 success + routing constraint + page-architecture build-inputs table corrected to describe the real behavior and the alias.
- (correction) retro-design-system parenthetical simplified to drop the 'byte-identical CSS' phrasing (Copilot re-flagged it); the invariant is stated purely as unchanged computed styles/render, extraction only moves rules + adds @imports.
- (correction) preview.html is NOT 'non-deployed': it is a Vite/Rollup input (vite.config.ts ~142-146), emitted to dist/ and served by Vercel (vercel.json deploys all of dist/), so it is reachable at /preview.html. The accurate framing: it is a test/screenshot harness, built and deployed but kept out of precache (globIgnored) and unlinked/unindexed (no sitemap, no nav). Corrects the earlier 'non-deployed' wording in the page audit and precache notes; page-architecture.md precache row reworded accordingly.
- (external, 2026-07-20) The routing-parity change (#503/#509, v1.67.0) RETIRES the "express transfers only through a sky lobby" divergence: reachability is now uncapped in both modes and no express transfer is lobby-gated, so that bullet is no longer a Classic/Modern difference. Applied directly to this spec's compareTemplate() (src/ui/templates/compare.ts): the "Freer building" bullet dropped its express clause and keeps only the escalator-office-floors divergence; compare.test.ts dropped the "switch elevators at any shared stop" phrase. The GameRules seam it keyed on was renamed expressTransferNeedsLobby -> walkwayWillingnessApplies (help.test.ts RULE_TO_HELP map updated to walkwayWillingnessApplies: null). New Modern divergence to add when #502 lands: a legible, TIME-based commute-comfort readout ("we warn you which tenants are suffering the trip"), NOT a transfer-count penalty.
- (decision) CAP-5 rescope (2026-07-20, owner call, follow-up after PR #517 merged): /help becomes the FULL how-to-play page, not comparison-only. The URL /help should host help; a comparison-only page under that URL is a naming mismatch. The guide sections (basics, going further, keyboard, about, report) are extracted from the Help modal (help.ts) into a shared single-source module src/ui/templates/helpContent.ts (HELP_SECTIONS + helpLede/helpAboutBody/helpReportBlock); the modal and the /help page both render it, so the guide copy has one source and cannot drift (same discipline as compareTemplate). Classic vs Modern is one section, anchored /help#classic-vs-modern; the in-game 'Open full page' link deep-links there. Non-goals unchanged (no SSR/prerender, no router). CAP-8 paired stills land on this page in the immediately-following PR.
- (decision) Clean-URL offline precache alias for /help is deliberately NOT implemented (decided in PR #517, recorded in the backlog; the spec Constraints + page-architecture.md now reconciled to match). A generateSW additionalManifestEntries/manifestTransform alias makes Workbox FETCH /help at SW install, which 404s wherever the Vercel rewrite is absent (vite preview, e2e harness), failing the whole precache and taking the game's own offline down with it. The built help.html is still precached (so /help.html is cache-served offline); the clean /help resolves online via the Vercel rewrite only.
- (decision) CAP-5 precache correction (2026-07-20 hotfix): help.html must NOT be precached. Workbox precaching defaults cleanURLs=true, so a clean /help navigation matches the precached /help.html and is served from cache, pinning /help to the SW-install snapshot and serving stale content across deploys (the /help page does not register the SW, so a /help-only visitor never updates it). Live symptom: after #519 shipped the full How-to-Play page, prod /help still rendered the pre-#519 comparison-only page. Fix: add help.html + its help-*.js page entry to Workbox globIgnores (mirroring gallery); keep the shared helpContent-*.js chunk precached for the in-game Help modal offline. Clean /help now always resolves over the network via the Vercel rewrite (fresh). Trade-off: /help is online-only (no offline copy), same call already made for /gallery. The earlier prescribed clean-URL precache ALIAS stays rejected (SW-install fetch 404s off-Vercel). navigateFallbackDenylist for /help is still required (it blocks the app-shell fallback; it does not govern the precache route).
- (decision) CAP-8 scene implemented (2026-07-20): scripts/scenes/classic-vs-modern.ts adds ONE new paired still, the escalator-on-office divergence (Modern escalators may serve office floors, Classic refuses), the clearest divergence not already shot elsewhere. Two mode-forked builders (buildEscalatorOfficeModern/Classic, seed 4600, identical tower) fail closed on the placement divergence: Modern asserts the office-touching flights placed, Classic asserts they were refused with the office reason, so a non-divergent pair can never ship. outDir features (2x), registered in screenshot-scenes.ts + the features shard. Verified by a host-browser dry-run (preview-only pixels): both frames staged cleanly and read as a clear pair (Modern shows 3 escalator flights climbing through the office floors + 'This tower: Modern' badge; Classic shows 1 lobby-to-shops flight + 'Transports 1' + 'This tower: Classic'). The other shortlisted cards (mode picker, pricing, schedule, stats) REUSE existing committed pairs on the /help page. Build-hint (hover refusal tooltip vs click toast) stays CAPTION-ONLY: the hover tooltip and the timed toast are transient, not a deterministic paired frame, so per media-plan.md 'no silent gaps' it falls back to a caption rather than shipping a mismatched or near-identical figure. Real committable PNGs come from the pinned container via the PR drift gate; the /help figure embedding is the PR's closing commit once those pixels land.
- (decision) CAP-8 closing move (2026-07-21, PR #527 review round): with the drift job's pinned-container captures committed to the branch, the escalator-on-office pair joined the /help figures as the FIFTH pair (per the party's embed-against-real-pixels ruling). The schedule figure's Modern half swapped from the 1x full-frame showcase hero (27-elevator-schedule) to the features-scale schedule-express capture so both halves of the pair sit at matching proportions; this also reconciles the code with media-plan's named reuse set (Copilot and the Blind Hunter review layer flagged the same mismatch independently). The gallery relabel drifted the sprite-gallery e2e visual baseline (648 px, the old link label), regenerated via the update-visual-baselines workflow ([update-baselines] marker), the only sanctioned mint path.
- (decision) Non-goal NARROWED for issue #516 (2026-07-21, owner-directed investigation then go-ahead): a build-time STATIC PRERENDER of the /help guide body into dist/help.html is now IN scope; SSR/hydration of app pages, a runtime SSR server, and prerendering any other page stay OUT. Feasibility probed hands-on in a worktree: @lit-labs/ssr 4.1.0 rejected (self-declared pre-release; plain Node dies on the PNG imports and lit-html's document reference; vite ssrLoadModule resolves images to dev URLs, never the built hashed ones); a Playwright post-build render rejected decisively (Vercel's build container runs npm run build with no browser). Chosen mechanism: a post-build script (scripts/prerender-help.ts) renders the BUILT help entry chunk under happy-dom (already the vitest devDependency, so zero new dependencies, browserless, Vercel-safe), rewrites asset hrefs to the built hashed ./assets/help-media/ URLs, and swaps the markup into dist/help.html's #app, replacing that file's noscript (src/help.html keeps its noscript for dev). No hydration: the client script clears the prerendered children and re-renders identical markup same-frame. The script fails loudly (guide-prose and asset asserts) rather than shipping an empty page, which is the drift guard. Verified: built page shows the full guide with images under JS-disabled Chromium; single render with JS on. Copy stays single-sourced in helpContent/compareFigures.
