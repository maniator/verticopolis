---
topic: Starter-lobby mode split: Classic canon-zero start, Modern keeps the centered seed
updated: 2026-07-22T02:18
---

- (direction) Owner asked for left-edge start for new towers next to the arrival plaza; brought to party per owner request (2026-07-21).
- (decision) Left-edge start REJECTED unanimously by the party: it flattens tower-shape variety, fights the bidirectional camera and build promise, and couples cosmetic scenery to sim geometry. Recorded as a non-goal with reasons.
- (decision) RATIFIED mode split (owner signed off): Classic founds canon-zero (1994 starts with zero built tiles; our 40-tile centered seed was silent training wheels), Modern keeps the centered seed as a documented nicety.
- (capability) CAP-1 Classic canon-zero founding: a new Classic tower has zero units; the player places the first lobby anywhere on the ground line exactly as in 1994.
- (capability) CAP-2 Modern founding unchanged: the centered 40-tile seeded lobby stays, documented as a Modern quality-of-life nicety (same family as build-refusal hints).
- (capability) CAP-3 Single seam: rules.starterLobby on the mode rule-set (GameRules, resolved once at founding) returns the seed placement or null; no ad-hoc mode checks.
- (capability) CAP-4 Classic onboarding beat ships in the SAME PR as the zero start: a lobby-placement card points a fresh Classic tower at the Lobby tool before other cards. VERIFIED FACT: no such card exists today; Onboarding.ts step 1 assumes the pre-seeded lobby (its comment says a ground lobby is pre-seeded and its copy says lay floor above your lobby), so the Classic flow needs the new first step and the existing steps' assumptions re-checked for an empty lot.
- (capability) CAP-5 First-boot camera opens on the lot center in both modes; a Classic empty lot must not open on a random corner.
- (capability) CAP-6 Help's Classic vs Modern comparison gains a line framing the split as a feature: Classic starts you the way 1994 did, an empty lot and your first decision; Modern starts with a ready lobby. The help drift-guard test gains the matching phrase.
- (constraint) PREP PR FIRST: decouple the golden-master fixtures and every integration test that assumes the centered seed (goldenMaster BUILD_SCRIPT builds at tiles 168-205 inside it) so the Classic re-pin is never tangled with a fixture refactor (Grumbal's two-re-pins-in-one-diff hazard). Fixtures assert their own lobby, the towerFixtures.ts pattern. Prep PR is internal-only, no version bump.
- (constraint) Golden masters move exactly as predicted: Classic hash re-pins with intent in the behavior PR; Modern hash must NOT move (the unchanged hash is the proof, per the demographic-routines precedent).
- (constraint) Save compat holds: only newGame changes; existing saves keep their towers; serialization and TDT import untouched.
- (constraint) Behavior PR takes a minor version bump (player-facing); review lane is /gds-code-review for both PRs (gameplay founding behavior).
- (constraint) Prose rules bind all new copy: American English, no em-dashes, no marketing vocabulary.
- (decision) Non-goals ratified: no left-edge start, no change to lobby placement rules or costs or the 375-tile lot canon, no scenery-to-gameplay coupling (the plaza stays pure render), no mode-split beyond the starter seed.
- (note) Companion test-impact.md holds the fixture-decoupling inventory. Refinement during derivation: goldenMaster switches to an ENSURE-lobby helper, so the prep PR moves NEITHER pinned hash (unchanged hashes prove the refactor behavior-free) and the behavior PR carries the Classic-only re-pin. Supersedes the input's implicit assumption that decoupling itself might re-pin.
- (assumption) Zero-unit serialized tower round-trips already (empty units array is legal); no save-format change needed for canon-zero founding.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1..6 each carry intent+success; constraints all bend decisions (two-PR sequencing, hash predictions, save compat, version, prose); non-goals explicit with reasons; success signal testable; IDs fresh.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing input claim landed in SPEC.md or test-impact.md, including the party's left-edge rejection rationale, the Grumbal sequencing hazard, the Onboarding.ts verified fact, and the golden-master proofs. Wrapper-only content dropped: none.
- (event) Spec finalized: 6 capabilities, 1 companion (test-impact.md), 1 assumption, 0 open questions.
