---
topic: A scheduled synthetic exception that proves error reporting is alive, so silence in the error stream is unambiguous
updated: 2026-08-09T12:42
---

- (note) Motivating incident: real $exception volume hit zero 2026-08-05 and stayed zero 4 days while boot volume held steady (~70/day before, ~62/day during); proving reporting was NOT broken took a multi-hour investigation.
- (note) Chain verified healthy this session: window error/unhandledrejection listeners + reportCrashException (src/analyticsErrors.ts) -> sendException -> sendToRelay (src/analyticsRelay.ts) -> POST /api/ingest -> PostHog project 524085.
- (note) Proven probe technique: Playwright chromium against https://verticopolis.com/ forcing WEBGL_lose_context produced both a crash event and an $exception (WebGLContextLost); a plain uncaught throw reached PostHog end to end on 2026-08-09 02:44 UTC.
- (capability) CAP-1: a scheduled run drives the real production origin in a browser and causes exactly one tagged synthetic exception to travel the whole client path; success = the event is present in PostHog and names the build that emitted it.
- (capability) CAP-2: the run verifies arrival within itself and fails loudly when the event does not land; success = a broken chain turns the scheduled job red rather than passing silently.
- (capability) CAP-3: canary events are trivially separable from real reports everywhere they are read; success = existing error and crash dashboards and any real-error alerting show identical numbers whether or not the canary ran.
- (capability) CAP-4: prolonged canary absence notifies a human; success = a canary that stops firing for the configured window produces a notification without anyone thinking to look.
- (constraint) Must exercise the real CLIENT path (browser -> relay -> PostHog), not a direct POST to /api/ingest: the incident's open question was specifically whether client capture still worked, which a server-side ping cannot answer.
- (constraint) A synthetic event indistinguishable from a real crash is a REGRESSION: crash reliability is already on dashboards (crashes-by-version tile, commits 95f5be14 and 0e07dcb6), so the canary needs a reserved marker AND exclusion wherever real errors are read.
- (constraint) No canary code may ship in the player-facing bundle or fire inside a real player session.
- (constraint) telemetryHostAllowed gates events to deployed hosts, so the canary must drive https://verticopolis.com; localhost and preview deployments send nothing.
- (constraint) Must not fight existing guardrails in src/analyticsErrors.ts: fingerprint dedup via a module-level seen Set, MAX_ERRORS_PER_SESSION=10, MAX_MESSAGE_LEN 500, MAX_STACK_LEN 2000, re-entrancy latch. One event per run, fresh session, stays clear of all of them.
- (constraint) Internal-only: no package.json version bump and no CHANGELOG entry (CONTRIBUTING.md line 298).
- (decision) Scheduling host RESOLVED to GitHub Actions cron: three scheduled workflows already exist (analytics-report.yml, engine-bench.yml, screenshot-approval-reaper.yml) and vercel.json declares no crons.
- (decision) PostHog verification credential RESOLVED: secrets.POSTHOG_PERSONAL_API_KEY already exists and is used by analytics-report.yml, alongside vars POSTHOG_PROJECT_ID (default 524085) and POSTHOG_HOST (default https://us.posthog.com). scripts/posthog-report.mjs is the query precedent to follow.
- (note) No existing workflow drives production with Playwright, so that part is genuinely new; e2e specs run against a local preview server.
- (note) Every event carries version at module load as of PR #801 (merged 44d8ada4), so a canary event names its build without extra work.
- (question) Alert mechanism unresolved: a PostHog alert on a canary insight (fires on absence, independent of CI) versus a second scheduled workflow asserting recent canary presence (keeps everything in the repo). Absence-detection is the harder half and PostHog alerts on absence need confirming.
- (question) Canary cadence unresolved: hourly bounds detection to ~1h but adds ~720 synthetic events/month against a real volume of 0-4/day; daily is quieter but a whole day of blindness. The right answer depends on how fast the owner wants to learn.
- (assumption) Assumed the canary should reuse the repo's own Playwright dependency and Node version pin rather than introduce a new browser-automation stack, since e2e already standardizes on Playwright 1.61.1.
- (event) Pass 1 (coherence): all 4 capabilities carry intent+success; intents state WHAT (drive, verify, separate, notify) not HOW; every constraint rules something out; 4 non-goals explicit; success signal is demonstrable against the 2026-08-05 incident. PASS.
- (event) Pass 2 (preservation): every measured fact from the input landed. Incident + steady boot volume in Why; chain and probe technique in Why/Constraints; host gate, guardrails, dashboard-corruption risk, no-player-bundle, internal-only all in Constraints; scheduling host and API key recorded as resolved decisions; version-on-every-event assumed into CAP-1 success. Wrapper-only content dropped: none. PASS.
- (event) Spec finalized: 4 capabilities, 0 companions, 1 assumption, 2 open questions (alert mechanism, cadence). Routes to /bmad-code-review on implementation.
