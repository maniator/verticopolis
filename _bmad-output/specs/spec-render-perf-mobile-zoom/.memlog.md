---
topic: Mobile render-performance fix: zoom-gated culling, shared room bakes, deferred hour reconcile
updated: 2026-07-15T02:41
---

- (note) Origin: owner report (Pixel 8a, fully zoomed out, top speed, stutter at morning/evening rushes) + profiled save sixseven_8.vctower (4-star, 5,085 pop, 20,155 units, 24 shafts, floors -8..91); diagnosis session 2026-07-14.
- (note) Diagnosis: sim exonerated (tick ~7.5ms/sim-min flat all day; 55-112ms total tick time per 30s gameplay window); frames ~1s at MIN_ZOOM under 4x throttle at EVERY hour from ~2,500 retained actors; 1,635 room actors each own a private ex.Canvas texture vs 8-16 mobile GPU texture slots per batch (ImageRenderer flushes per slot exhaustion, Excalibur 0.32); hourly onHour scans (50-110ms) share a frame with the full render reconcile.
- (decision) Party-ratified (Cloud/Link/Samus, Grumbal walk-on): three stories in order S1 zoom cull, S2 shared bakes, S3 deferred hour reconcile; engine onHour amortization REJECTED as rider (determinism/golden-master load-bearing), backlogged pending checkpoint-inputs design.
- (capability) CAP-1 (S1): below a named sub-legible zoom threshold, crowd/vehicle actors (people, walkers, elevator cars, train, trucks, garage cars) are culled AND their per-frame position/graphic loops in updateMotion/reconcileCrowd are skipped; hysteresis (re-show above a higher zoom than hide) prevents strobing at the boundary. Success: rush probe on the owner save at MIN_ZOOM shows the crowd/vehicle share of frame time eliminated; actors reappear correctly when zooming in; no strobe across the threshold.
- (capability) CAP-2 (S2): room actors share IMMUTABLE baked canvases keyed by the existing draw signature (refcounted cache, evicted at zero refs); a unit look change swaps its bake reference and never mutates a shared canvas; animated rooms keep private canvases. Success: e2e visual baselines byte-identical; distinct room textures collapse from ~1,635 (per unit) to the live signature population (dozens); rush-probe frame times at MIN_ZOOM drop materially on the owner save.
- (capability) CAP-3 (S3): the hour-boundary full render reconcile (syncScene on d.hour change) defers one frame so it never shares a frame with the engine onHour scans. Success: the on-the-hour longest-frame in the rush probe drops by roughly the reconcile share; no visual state is ever skipped (the reconcile still runs, one frame later).
- (constraint) Render/presentation-only: zero engine-number changes, engine stays DOM-free, golden master byte-stable, canon parity untouched (Samus condition: a camera-and-paint patch).
- (constraint) Cull threshold is a named constant with the sub-legible rationale (figure under ~3 screen px) written beside it; walker BUILD stays structural-revision-gated (never rebuilt on zoom changes) per Cloud.
- (constraint) Shared bakes are immutable by contract (Grumbal trap: mutating a shared canvas repaints every sharing unit); signature under-keying is guarded by the pinned visual baselines failing the PR.
- (constraint) Per story: all four quality gates + the CI perf gate; /gds-code-review with findings fixed and defers backlogged; rush-probe before/after on sixseven_8.vctower in the PR body.
- (note) Non-goals: engine onHour amortization (backlogged, needs determinism-safe checkpoint design); LOD for room/structure sprites (rooms stay drawn at all zooms, only crowd/vehicles cull); any gameplay/balance change; WebGPU or Excalibur upgrades.
- (direction) Owner greenlight 2026-07-14: investigate with game agents/skills, spec with agents/skills, implement the mobile stutter fix.
- (event) SPEC.md + diagnosis.md derived (3 capabilities, 5 constraints, 3 non-goals). Self-validate pass 1 (coherence, Spec Law 1-6+8): every CAP has intent+success, intents are WHAT-only, constraints all bend design (immutability, threshold naming, build gating, PR process), non-goals explicit, success signal testable via the rush probe; lean. Pass 2 (preservation): diagnosis numbers, mechanism, party conditions (Samus threshold/engine strings, Grumbal immutability trap, Cloud walker-build gate), verification harness all landed in SPEC or diagnosis.md; nothing load-bearing dropped. Verdict: PASS both.
- (note) Replaced the template preamble em-dash in SPEC.md line 9 with a colon per the repo no-em-dash rule (gds-code-review Acceptance Auditor V4); future derives should keep the colon form.
- (event) CAP-2 assumption falsified by measurement: pixel-hash census of the owner save's 1,635 room actors gives 738 unique bitmaps at day (hour 10) and 1,302 at night (hour 20); the lit-only windowView sparkle (geoVariant axis n=997) makes offices 555/557 and condos 431/431 unique at night, so signature-keyed sharing cannot collapse textures near the 8-16 mobile GPU slots per batch. Party reconvened to pick a replacement design for CAP-2.
- (note) Census per kind (day unique/actors): office 120/557, condo 357/431, hotelSingle 104/164, hotelDouble 79/107, hotelSuite 47/74, parking 8/244, housekeeping 1/18. Probe: temporary e2e/textureCensus.spec.ts pattern (rasterized rec.cv bitmaps hashed via toDataURL FNV-1a).
- (decision) Party ruling (unanimous, 2026-07-15): CAP-2's mechanism becomes region composition. Rooms draw into a fixed world-space region grid (one cached ex.Canvas + anchor actor per region holding settled units, clipped per-unit draws at integer offsets, the transportGraphic band precedent); animated rooms (fire/construction) keep private actors and their region leaves their footprint unpainted; a signature flip marks the unit's region(s) dirty; per-unit room actors retire and picking resolves via tower.unitAt (the floor/lobby grid-lookup precedent, footprint-indexed across width and height by Tower.register). Success criterion unchanged: baselines byte-identical, distinct room-layer textures collapse to the live region count (order dozens), material min-zoom probe drop.
- (constraint) Bounded drain is load-bearing, not tuning: dirty regions repaint through a budgeted queue (at most a named small count per frame, visible-first) so no frame ever rasterizes/uploads the whole tower; naive repaint at the 17:00 lit flip is ~40 full-region uploads (~50MB) in one frame, worse than the status quo. Exceptions: animated-state transitions repaint their region same-frame (no fire ghost), and initial load may drain fully before the first presented frame.
- (constraint) Region dimensions are named constants with both pixel sides pinned <= 2048 by a unit test (the TRANSPORT_BAND_FLOORS precedent); exact values (candidates 64x16 tiles/floors vs 32x20) are chosen by the upload micro-bench, not by taste.
- (decision) Story order re-cut: CAP-3 (defer hour reconcile) now lands BEFORE the region story to isolate hour-boundary attribution; a small picking-via-unitAt story lands next (deletes the O(rooms) pointer scan, shrinks the region diff); then regions + budgeted drain as one story; an optional drain-tuning story only if the probe demands it.
- (constraint) Pre-region gates: (1) one-region pixel-diff spike in the pinned Playwright container proves byte-identity before the story starts; (2) a texture-upload micro-bench under 4x throttle prices region size; (3) a blame-split probe (all rooms sharing one bake vs all room actors hidden) decomposes texture flushes vs actor overhead; (4) a full-tower day/night scene baseline is minted on main via the sanctioned workflow BEFORE the region diff exists, because the current pinned baselines image single gallery units and cannot catch region seams or layering.
- (note) Owner acceptance riders (designer): watch both rushes AND the 17:00-19:00 sunset window on the physical Pixel 8a; a zoom-in pass mid-rush (tap, inspect, variety reroll) must feel identical; fire drill on device (no ghost, no hole, no seam); expect a residual on-the-hour engine tick (the backlogged onHour non-goal) to surface once frames stop hiding it; probes report the hour-boundary frame SEQUENCE, not just the longest frame.
- (event) Spec re-derived after the party ruling: CAP-2 intent rewritten to region composition (ID preserved), story order re-cut (CAP-3 then picking then regions), bounded drain + region pins + pre-region gates + owner sunset acceptance added as constraints, shared bakes and sparkle bucketing recorded as non-goals, region-design.md added as a spec-authored companion carrying the engineering shape (invariants I1-I4, seams, lifecycle, cost model). Self-validate: PASS on coherence (IDs stable, intents paired with success, constraints bend design, non-goals explicit, success testable) and PASS on preservation (party verdicts land in SPEC constraints, diagnosis census section, and the companion; the verdicts' persona prose is wrapper, dropped).
- (event) Pre-region blame-split probe ran (owner save, min zoom, 4x throttle, sim held): hiding all room actors saves only ~100-270ms of a ~1,100ms frame, while deactivating the struct TileMap drops p50 to ~350ms; transports are negligible. On the CI-class container (software GL) the 18,520 on-screen tile cells dominate the min-zoom frame, not the room texture population.
- (note) Measurement-venue caveat now on record: container probes rasterize in software, so unique-texture batch breaks are cheap there and tile quads are expensive; on the Pixel 8a's hardware GPU the weighting likely inverts. Container probes stay authoritative for hour-boundary attribution and relative experiments; CAP-2's min-zoom win is judged on the owner's device (already an acceptance constraint). Region-story container numbers will be understated; do not read that as failure.
