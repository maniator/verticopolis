---
topic: Smooth elevator car motion (GH #688, Reddit report 2026-07-28)
updated: 2026-07-29T03:11
---

- (direction) Owner directed (2026-07-28): file #688, spec it, implement the fix; source is a player report on the 2.0 r/tycoon thread (ST cars had smooth accel/decel, ours stutter)
- (note) Diagnosis: frameLoop ticks sim only on whole accumulated game-minutes (2-4Hz at normal pace), updateMotion draws cars raw from carPositions each 60fps frame (towerCrowd.ts:340), so cars freeze-and-jump; breathing clock widens gaps at lunch crush
- (note) Second half: engine moves cars at constant CAR_FLOORS_PER_MINUTE (ElevatorDispatch.ts:185) and stops dead at dwells; the 1994 original eased in and out
- (constraint) Sim untouched: carPositions, dispatch, dwell, arrival times byte-identical; golden master must not re-pin; easing lives entirely between sim value and pixels
- (constraint) Determinism: interpolation advances on the engine update's elapsedMs (stepped by the screenshot TestClock), never wall clock; pinned-container gallery and visual baselines stay reproducible frame-for-frame
- (constraint) Car draw motion is FUNCTIONAL motion, not decoration: must not gate on the d.anim decoration clock (which freezes when animation is off); reduced-motion players still see cars glide, matching walkers
- (constraint) Cab indicators (riders bucket, lantern, FULL) keep reading live sim state; interpolation moves only the drawn y
- (capability) CAP-1 cars glide: per-car drawn position pursued toward sim position every frame, no freeze-and-jump between sim ticks
- (capability) CAP-2 accel/decel ease: departures ramp up, arrivals settle, matching the original's feel; no overshoot past the target floor
- (capability) CAP-3 snap on discontinuity: teleports (motion resync, load, big camera/state jumps) snap the drawn position instead of gliding across the tower
- (decision) Version bump minor (player-noticeable feel change) with a one-line CHANGELOG note; /gds-code-review is the review skill
- (event) Self-validate pass 1 (coherence): 3 capabilities with intent+success, 5 design-bending constraints, 3 non-goals, concrete success signal; no assumptions or open questions left
- (event) Self-validate pass 2 (preservation): issue #688 diagnosis, code anchors, determinism/reduced-motion/indicator constraints all landed in SPEC.md or brownfield.md; Reddit quote kept in Why
- (event) gds-code-review round 1 (3 layers) + owner field report (Pixel 8a express teleport): confirmed night-pace saturation (VMAX 20 < 26 fl/s, trail eq 7.3 > snap 6), 1ms clamp starvation, MAX_STEP 50ms outpaced on 50-200ms frames, per-edit syncMotion snap, reverse-momentum excursion, >200-char changelog note, vacuous glide test, unwired renderer tests; all patched (VMAX 30 / TAU 0.18 / ACCEL 200, clamp-artifact dt, momentum snap, draw-state carry-over by shaft id, shaft-span clamp, shorter note, wiring tests in towerCrowdMotion.test.ts); dismissed with evidence: cull re-entry, determinism legs, visual baselines, fast-forward semantics
