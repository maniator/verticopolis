---
topic: Split all source and test files over 500 lines into cohesive <500-line modules behind stable barrels, adding shift-left/regression nets, with zero behavior change
updated: 2026-07-15T00:00
---

- (direction) Split all 18 source files >500 lines and 8 test files >500 lines into cohesive <500-line modules; user asked to do it with dev/ux/game party help, write a spec, and implement in same session.
- (decision) Refactor is behavior-preserving only: no player-facing change, therefore NO package.json version bump (internal-only per CLAUDE.md versioning rule).
- (decision) Barrels are sacred: every file currently imported by others keeps its path and every exported name; only internal layout moves. Applies especially to facilities.ts, Simulation.ts, pixelSprites.ts, sprites/structure.ts, tdtImport/Export/Format.ts, SaveGame.ts, UI.ts, ToneAudioEngine.ts, screenshot-builders/scenes.ts, TowerEngine.ts.
- (decision) Nets-before-knives: write shift-left/regression guards FIRST and green before any file moves - golden-master Simulation determinism snapshot, a repo file-size guard test (<=500 lines), barrel-surface tripwire tests, and ByteWriter unit tests.
- (decision) Five-stage risk-ascending execution, gates green + one commit per stage: (0) nets, (1) pure leaves, (2) storage/TDT, (3) engine friend-modules, (4) shell UI/main/TowerEngine, (5) test-file splits + flip size-guard allowlist to empty.
- (constraint) TDT export idempotence stays byte-identical: buildTDT output must re-import with ZERO importer warnings and re-export byte-for-byte identical; export inverts import's shared semantic tables from ONE source (extract to leaf tdtTables.ts so reader+writer cannot drift).
- (constraint) Rendering stays pixel-identical: do NOT re-mint e2e/visual.spec.ts-snapshots or docs/screenshots; draw refactors move whole functions verbatim with explicit params. Local host-browser captures are not authoritative (pinned-container rule).
- (constraint) src/engine/ stays free of DOM/rendering; canon build caps + transport pooling stay in the facilities barrel (BUILD_CAPS/POOLED_CAPS/MAX_CARS/maxSpanFor as single source of truth).
- (constraint) Screenshot builders stay self-contained: every exported fn is serialized via page.evaluate().toString(), so NO shared module-scope helpers or cross-function calls; split by moving whole functions only, duplication is required not a smell.
- (constraint) Preserve the two type-invisible contracts: window.game runtime surface (sim/engine/speed/grid + any-cast selectPicked/selected/refreshEditor read by e2e/scripts) and the renderEditor(key,build,volatile) volatile-patch protocol between main.ts and UI.ts.
- (decision) Stateful giants (Simulation, Tower, TowerEngine, Crowd) use friend-module pattern: free functions in sibling files taking the instance; touched private fields relax to internal with @internal tag; NO new subagent/controller object hierarchies (that is redesign, not refactor). deserialize() is NOT churned.
- (decision) buildTDT (815-line single closure) gets a real gather->encode->report decomposition with named intermediate types and a ByteWriter class mirroring tdtFormat ByteReader (incl. setHdrU16 back-patch); safe only because byte-identical idempotence net catches drift. ByteWriter gets its own unit tests.
- (decision) Test files split by moving WHOLE describe blocks verbatim; vitest total test count must be identical to the digit before and after each split (parity check is the guard). Test splits run LAST so any failure during source work localizes to source.
- (capability) CAP-1
- (capability) CAP-2
- (capability) CAP-3
- (capability) CAP-4
- (capability) CAP-5
- (capability) CAP-6
- (capability) CAP-7
- (assumption) faqComplete.test.ts, calendar.test.ts, tower.test.ts, gameControllers.test.ts and the other >500 test files are in scope for splitting under the same whole-describe-block + count-parity rule as the source files.
- (assumption) src/styles.css (1522 lines) is out of scope: the 500-line rule as stated by the user targets code files (design-system.md treats styles.css as ONE intentional generation of CSS); flag rather than shard it.
- (note) Per-file module boundaries captured from four scout briefs live in this session context and the companion split-plan.md; SPEC cites the companion.
- (event) Self-validate Pass 1 (coherence): PASS - 7 capabilities each carry intent+success; constraints bend decisions (no version bump, DOM-free engine, self-contained screenshot builders, type-invisible contracts, staged execution); 5 explicit non-goals; success signal is runnable (4 gates + diff-stat + empty size-guard).
- (event) Self-validate Pass 2 (preservation): PASS - every party ruling landed (barrels sacred, nets-first, mechanical-by-default with buildTDT/TowerEngine exceptions, friend-module pattern, hard bans, test count parity, both review skills). One open question logged re: size-guard exemption fallback for irreducible files.
- (event) Spec finalized: SPEC.md (7 caps) + split-plan.md companion + adopted project-context.md.
- (event) 2026-07-15 delivery true-up: the split waves shipped to main 2026-07-13/14 (storage/TDT split re-derived on #209, Crowd #218, Simulation #226, ToneAudioEngine #250, UI #252, facilities sprites #253, TowerEngine #256, plus the pixelSprites and screenshot-builder splits), with the golden-master, barrel-surface, ByteWriter, and file-size guards live in CI. Residual: `src/tests/fileSize.ratchet.txt` still exempts 11 files (src/main.ts, src/engine/Tower.test.ts, and 9 oversized integration test suites), so the "empty allowlist" success signal is not yet fully met; the ratchet guard prevents regressions and new entries.
