---
topic: Traffic HUD indicator: fix always-Smooth + name the worst floor
updated: 2026-07-15T00:00
---

- (event) Spec created headless from a settled game-design/UX party pressure-test; all decisions pre-made, no elicitation.
- (note) Bug: Traffic chip (src/main.ts updateTraffic ~L403) reads sim.congestion() (mean over populated+served floors, /12 headroom); thresholds Busy>=1.0/Backed up>1.25/Gridlock>1.6 sit above the scale the engine reaches, so it reads Smooth ~always.
- (note) Internal contradiction: congestion overlay LEGEND already reads peakCongestion(); the chip reads the average — two HUD readouts disagreeing.
- (note) MEASURED: 7 real saves of same tower peak 0.17-0.29 (genuinely smooth). Starving shafts strands floors (unreachable, drop out of calc); peak only 0.29->0.41. avg vs peak diverge only on a localized jam among healthy floors: avg 1.15 vs peak 3.11. Peak dynamic range 0.17->3.1+.
- (decision) CAP-1 signal: chip reads peakCongestion() (busiest populated+served floor) matching overlay legend + 1994 spatial cues (red walkers, congestion heatmap). Mode-agnostic parity fix.
- (decision) CAP-2 thresholds on peak (measured, hysteresis +-0.03 kept): Smooth <0.4, Busy 0.4-0.8, Backed up 0.8-1.5, Gridlock >1.5.
- (decision) CAP-3 modern readout: chip also names the worst floor, e.g. 'Backed up 42F' (what 1994 could not do). New player-facing capability.
- (decision) CAP-4 decouple + honest docs: rewrite traffic.ts false comment claiming tier-2 aligns with walker-red gate; chip=early tower warning, red walkers=acute per-person symptom via stress=clamp(congestion-1).
- (constraint) DO NOT modify stress=clamp(congestion-1) or any economy/satisfaction math; only the chip's input signal, thresholds, comment, and label are in scope.
- (constraint) src/engine/ stays DOM/render-free; the worst-floor label formatting lives in the UI layer (main.ts), engine exposes the floor number.
- (constraint) Quality gates green (typecheck/lint/test/build); bump package.json version; /gds-code-review mandatory before push (project non-negotiable).
- (constraint) Change is mode-agnostic (identical Classic/Modern); no per-mode branching in the sim.
- (note) Non-goal BENCHED: Modern-only softening blend max(avg,0.7*peak), playtest-gated only if straight peak too twitchy.
- (note) Non-goal FOLLOW-UP: tap-chip-to-fly-camera-to-hotspot (touches input/camera).
- (assumption) peakCongestion()==0 when no floor is populated-and-served (new/all-stranded tower) reads Smooth; unchanged from today, owned by the reachability system, not fixed here.
- (event) Decisions logged; rendering SPEC.md next.
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS. 4 capabilities each with intent+success; constraints bend design; 3 non-goals explicit; success signal demonstrable against towerone_6; IDs unique; lean.
- (event) Self-validate Pass 2 (Preservation): PASS. All load-bearing input claims (signal, thresholds, decouple, modern worst-floor, benched blend, follow-up, known gap, gates) landed in SPEC.md or its Assumptions/Non-goals. No wrapper-only drops of substance.
- (event) Spec finalized.
- (event) 2026-07-15 delivery true-up: shipped to main 2026-07-08 in the PR #171 merge (commit ae3e798). The chip now reads `Simulation.peakCongestionHotspot()` (src/main.ts updateTraffic, src/engine/traffic.ts) with the measured peak thresholds and names the worst floor ("Backed up · 42F"); the stress math is untouched as constrained.
