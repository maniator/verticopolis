---
topic: Splash-screen mute control (Reddit request, owner-approved)
updated: 2026-07-22T18:47
---

- (direction) Owner approved on Reddit (2026-07-22): add the mute to the splash; in-game topbar already has one
- (decision) Party-ratified design (Samus/Sally/Cloud/Indie roundtable): visible corner toggle on splash binding the existing persisted master-mute; theme stays, no removal of splash music (Samus veto, Sally concur)
- (capability) CAP-1 splash mute toggle button, top-right, box-art styled, focus-trap member, aria-pressed, >=44px target, icon flips
- (capability) CAP-2 one mute source of truth: splash button drives audioPrefs.toggleMute (same prefs.muted the topbar toggle writes); sliders untouched; unmute restores prior mix
- (constraint) main.ts:209 already applies prefs.muted at construction BEFORE any music program starts, so returning-player silent boot ALREADY holds; spec pins it with a regression test rather than rebuilding it
- (constraint) Design-system splash exemption: the splash is box art and stays un-unified; the button must NOT use the in-game .btn chrome
- (constraint) toggleMute calls audio.start(): acceptable on splash (the click is the gesture); starting muted is silent
- (decision) Non-goal: removing or default-muting the splash theme (identity per party ruling); non-goal: per-channel controls on splash (Settings owns sliders)
- (assumption) The audio facade gates program playback while muted regardless of program-set order (verified at main.ts:209 ordering; re-verify in implementation)
- (note) Success signal: a returning player who muted once boots to a silent splash with a visibly muted control; toggling on splash is reflected in the topbar toggle after dismissal
- (event) Self-validate pass 1 (coherence): all 8 Spec Law rules hold; 3 capabilities each with intent+success; 4 constraints all design-bending; 3 non-goals; concrete success signal; 1 assumption (facade mute-vs-program order), 0 open questions
- (event) Self-validate pass 2 (preservation): all party-design claims and code-verified facts landed in kernel; no companion needed beyond adopted project-context.md (no catalogs/diagrams); Reddit thread wrapper prose dropped as ceremony
- (event) Assumption RESOLVED: AudioFacade.start() applies this.muted to the impl before impl.start() (Audio.ts:105), and setMuted stores facade-level regardless of impl; mute/program order cannot leak sound
- (event) Implementation complete on branch claude/splash-mute: CAP-1 splash-mute button (Onboarding template+wiring, .splash-mute CSS 48px), CAP-2 one-truth binding (appBoot opts -> app.toggleMute; toggleMute now calls ui.setAudioGlyph so the topbar glyph follows every caller; the stale-glyph gap was found and closed), CAP-3 pinned at the facade (order-independent muted splash boot). Tests: onboardingSplash.test.ts (6), audioPrefs glyph pins, audioFacade CAP-3 pin, uiDialogs contract update. Full suite 2792 green; v1.86.0
- (event) Review loop converged (bmad-code-review, 3 rounds): R1 6 findings fixed (aria double-signal, reading order, tautological test, 44px pin, reload round-trip pin, inert-button); R1.5 owner-directed refactor (all splash buttons to lit @click); R2 2 test findings fixed (return-value channel + reload single-toggle assert); R3 confirming pass CLEAN on Blind Hunter + Acceptance Auditor. 0 defers.
