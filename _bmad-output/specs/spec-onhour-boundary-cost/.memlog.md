---
topic: Engine hour-boundary cost: cache the noise-adjacency scan, hoist served-set reads, outcome-identical
updated: 2026-07-15T15:40
---

- (event) Initiative opened: owner accepted CAP-2 (render-perf) on the Pixel 8a; the residual on-the-hour hitch moves to the engine side. Lineage: spec-render-perf-mobile-zoom recorded onHour amortization as a non-goal; backlog consult row onhour-amortization-consult (GH #367) stamped spec-first, never a rider.
- (note) Diagnosis (headless node, post-CAP-2 main 959c1d1, owner save 20,155 units model v2): regular minute tick 8.35ms avg; hour-boundary tick 52-113ms; worst 8:00 where hotelCheckout 22ms + dispatchHousekeepers 18ms stack. Phase split per boundary: updateSatisfaction 20-41ms, attemptMoveIns 5-11, evaluateStar 3-10, collectTrafficIncome 3-8, updatePresence 2-5. Browser probes are render-swamped; headless is the authoritative venue for engine attribution.
- (note) Inner attribution across 24 boundaries: noiseAfflicted 469ms (uncached, ~70% of the satisfaction sweep; up to 22 tiles x 2 directions of string-keyed map probes per sensitive unit, re-run in vacateCause for bottomed-out units); isFloorServed 200ms and nearestTransportDistance 7ms are ALREADY revision-memoized (servedFloors/transportColumns), their measured cost is call-path and instrumentation overhead; spatialCongestionByFloor 90ms is demand-driven and stays live.
- (decision) Party ruling (2026-07-15, architect REVISE-narrowed, dev and designer concurring): ONE new cache only, a per-revision noise-adjacency index in a new pure module src/engine/sim/noiseIndex.ts; the served-set and transport-distance caches proposed in the brief are struck as duplicates of shipped revision-memoized caches; their cost is recovered by hoisting tower.servedFloorSet() out of the per-unit loops (updateSatisfaction, attemptMoveIns, ElevatorDispatch.accumulateWaiting) and passing farWalk/noisy through to vacateCause instead of recomputing.
- (decision) Story cut: ONE PR carrying the noise index, the hoists, the vacateCause pass-through, the differential/state-blindness/mutator-sweep tests, and a committed opt-in headless bench; splitting would split the review context of a single mechanism and invariant.
- (capability) CAP-1 hour-boundary cost: the hourly engine bundle stops rescanning layout-derived predicates, cutting the boundary tick to a few multiples of a regular minute with byte-identical outcomes; success = bench shows typical boundary ~62ms to ~30-35ms and 8:00 ~113ms to ~85ms, golden master hash unmoved, owner cannot feel the hour flip on the Pixel 8a.
- (constraint) Outcome-identical or it does not ship: golden master PINNED_STATE_HASH must not move; no RNG draws added, removed, or reordered; same toasts, notices, evictions, star math. Determinism-safe means the cache changes WHEN work happens, never WHAT is observed: layout predicates are only ever consulted at boundaries, and every layout mutation bumps tower.revision before the next boundary, so cached and fresh reads see the same world.
- (constraint) Cache storage is instance-attached (Tower-field friend pattern like servedSet, or a Simulation transient like elevatorUtil), revision key strict-equality initialized to -1. A module-level singleton keyed by bare revision is forbidden: revision is a per-instance counter and two Tower instances collide.
- (constraint) Never cache state-reading predicates. The functionalParkingSet precedent (tower/routing.ts:118-125) is the fence: fire/gut/construction/occupancy mutations do NOT bump revision. noiseAfflicted is cacheable only because it is state-blind, including that a gutted or empty room still radiates by kind; that behavior must not be cleaned up in passing.
- (constraint) Caches never serialize: serialization is field-explicit today; the PR adds a schema guard asserting no cache key appears in serialize() output and towerStateSig is unchanged by cache warm/cold.
- (constraint) spatialCongestionByFloor stays live (reads live demand); if anyone ever memoizes it the key is revision + clock.hour per the 2026-06-30 final review.
- (constraint) The 8:00 stack is canon and untouchable: morning hotel checkout is PRD FR-13 parity (review F37 moved it off midnight), checkout opens the housekeeping shift (FR-14, EconomySystem coupling comment), and overnight guests must be present at the midnight TOWER/VIP census (F33). The hitch is made cheaper, never rescheduled.
- (constraint) Mandatory differential test: on randomized towers every sensitive unit asserts cached noise flag === direct nearestKindWithin, covering the multi-story cinema upper-story source (register maps both stories into rooms; a per-floor source paint that misses it is the top-ranked silent-drift risk), lobby-shield break BEFORE source check, open-air gap break, shared-wall distance 0, and exact band edges 11/21. Plus state-blindness pins (fire/gut/finishConstruction/occupancy leave cache === fresh) and a mutator sweep asserting all eleven revision++ sites bump (place, removeUnit, placeTransport, removeTransport, resizeTransport, setCars, setStop, clearStops, setExpressStops, coerceExpressStops, reindex), turning the hand audit into CI.
- (constraint) The perf claim must be reproducible: the PR commits an opt-in (env-gated, never a CI gate) headless hour-cost bench over a synthetic ~20k-unit fixture; the owner save is personal data and stays out of the repo. Process: four quality gates, /gds-code-review in-session, patch version bump (player-noticeable hitch reduction).
- (decision) Non-goals ruled explicitly: sweep slicing across sim minutes AND onHour phase staggering (checkout :00 / housekeeping :01 / satisfaction :02) are both determinism-breaking and parked behind the GH #367 consult, reopened only by a post-cache Pixel 8a trace still showing a hitch attributable to the sim tick; touching the 8:00 hotel bundle; caching congestion; a nearestTransportDistance cache (0.3ms/boundary residue).
- (note) Owner acceptance (post-merge, on device): at top speed on the big save, watch 12+ consecutive hour flips spanning 6:00-9:00 plus one midnight (onDay stack), at min zoom AND detail zoom; the flip must not be feelable except by the clock. One manual invalidation spot-check: pause, drop a fastFood beside an occupied office, unpause, the noise notice still lands at the next boundary.
- (note) Expected arithmetic (dev): savings ~19.5ms (noise memo) + ~6-7ms (served hoists, which also trim the flat minute tick via the per-step dispatch site) + ~0.5ms (vacateCause fold) = ~27ms; residual 8:00 cost is the canon hotel bundle plus state-dependent population scans.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1 carries intent+success; every constraint rules something out (one-cache scope, instance attachment, state-blind fence, serialization guard, canon 8:00, mandatory tests, reproducible bench, one PR); four explicit non-goals; success signal testable (bench numbers, device protocol, golden master). Engineering shape appears in constraints deliberately: the party ratified the mechanism, so it is contract, not decoration.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing party claim landed in SPEC.md or diagnosis.md (numbers, attribution, revision audit, cinema drift risk, expected arithmetic, GH #367 lineage and reopen gate, owner acceptance protocol). Wrapper-only content dropped on the record: the three verdicts' persona prose; the ratification outcomes are summarized in the companion.
- (event) Spec finalized pending owner review; implementation explicitly gated on the owner's go-ahead (their 'sure' authorized the spec, not the build).
